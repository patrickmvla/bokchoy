---
vault_version: 2
---

# BokChoy vault index

## marketing

- [[marketing/currencies-endpoint-research]] — type: research — **`GET /v1/currencies` wire shape + field set RESOLVED 2026-05-14.** Triggered by /implementation seat hitting two contract gaps on Slice M-1; /design seat staked Position D (rename column `code → slug` + tighten regex to kebab) on Better Auth + Vercel cites; handed to /research because those cites are dev-tool organizational/account identifier class, not currency class. 9 sources surveyed across two classes. **F1 LOAD-BEARING:** game-economy SDK class (BokChoy's class) converges on `code`-shaped wire field for the customer-defined currency identifier. **4 production cites:** LootLocker (`code`), PlayFab Economy v2 (`FriendlyId` with prose "the currency code"), RevenueCat (`code`), AccelByte (`currencyCode`). **Tier 2 ≥ 3** (LootLocker + PlayFab + RevenueCat at official docs; AccelByte tier-4). **Zero cites for `slug`** in this class. **F2:** generic dev-tool class (Better Auth orgs + Vercel teams) uses `slug` — but only for organizational/account identifiers, NOT currencies. The design seat's Position D conflated these classes. **F3:** Stripe `currency: 'usd'` is a property convention (not a resource identifier) — coherent at a separate layer from the resource wire-field-name decision. Confirms BokChoy's SDK customer arg `currency: 'gems'` is correctly Stripe-style. **F4:** response field set varies; `decimals` is NOT universally exposed on currency-list endpoints. Audience-scale-matched precedent (RevenueCat + LootLocker — SMB/indie scale, BokChoy's class) ships minimal field sets. AccelByte + PlayFab (larger-studio scale) ship richer. **Contradiction probe** (active search for game-economy SDK using `slug`) returned empty — WebSearch synthesis quote: *"slug isn't prominently featured as a standard field name for virtual currency identification."* **Operational implications:** Gap 1 → adopt **Position A** (wire = `code`, schema column unchanged, NO migration); the design seat's Position D is superseded. Gap 2 → adopt **Position α'** (4 fields: `{ id, code, displayName, createdAt }`); decimals/premium/tradable deferred to field-additive amendments when justified. Cascade: minor wording amendment owed to `[[marketing/v1-shape]]` Mitigation #1 (clarify "slug-resolution" is a generic-English term; wire field is `code`). 3 open threads (Beamable schema not resolved; AccelByte tier-4 needs direct-fetch upgrade; SDK customer-arg `currency` vs `currencyCode` is a separable /design question). Confidence: high. **Unblocks Slice M-1 for /implementation** once /design writes the v1-shape amendment.
- [[marketing/v1-shape]] — type: decision — **BokChoy marketing v1 shape RESOLVED 2026-05-14 (amended same day to add features section + page structure).** Five sub-decisions form the v1 marketing surface: **(i) IA depth = 3 marketing routes** (`/`, `/pricing`, `/docs`) per Clerk/Resend tight pattern of `[[marketing/landing-patterns-research]]` F5. Dropped at v1 with content-availability revisit triggers: `/changelog` (third release), `/blog` (first post), `/customers` (third customer), `/security` (SOC2), `/build-vs-buy` (named-competitor + F8a conditions). Top-nav 4 items (Product/Pricing/Docs/Sign in). **(ii) Hero copy shape = α (noun-claim + outcome).** User defense: cognitive anchoring for cold visitors — noun-claim provides a category placement that lets visitors-without-prior-context place the product in mental map in seconds; outcome-only (β) skips the anchor and sub-headline can't recover. Production-cited × 5/9 sites (Stripe, Resend, Lootlocker, Supabase, htmx). **(iii) Hero visual = code-as-hero, V2 variant (translated SDK).** Friendly names in the snippet (`player: 'player_123'`, `currency: 'gems'`, `reason: 'level_up_reward'`), NOT raw UUIDs. User defense: V1's marketing is throwaway (UUID leaks internal implementation → first-impression damage); V2's wait-cost (SDK slug-resolution layer + cache + lookup-failure handling, ~2 weeks bounded) is lower than V1's rebuild-cost. Stripe pattern verbatim (`currency: 'usd'`). **(iv) Trust-signal substitute strategy = audit-log-primary 3-pillar features section.** Lootlocker pattern (3 pillars). PRIMARY: audit-log replayability (with idempotency rolled in) — answers the most-asked support question in any wallet/ledger product ("player says they didn't get their gems — what happened?"). SECONDARY: Postgres-native (no NoSQL lock-in). TERTIARY: defense-in-depth (4 layers). Pillars are explicitly NOT equally weighted. User defense: recommendation was research-grounded. **(v) Page structure below hero = S-walkthrough.** 5 sections: hero + 3-pillar features + code walk-through (3-4 sibling SDK operations) + CTA strip + footer. The code-walk-through doubles down on the code-as-hero V2 commitment (visitors who read hero code want MORE code). S-tight (no walkthrough) rejected as under-supporting; S-stripe (use-case segments) rejected on audience-fragmentation. **5 rejected alternatives (now 7):** (IA-Stripe) Stripe-depth IA — falsely implies product breadth at single-product scale. (IA-htmx) single-page — reads as incomplete commercial site. (β) outcome-only — no pre-existing mental category for "wallet primitives for games." (V1) UUID-flavored snippet — first-impression damage. (V3) defer code-as-hero — forfeits highest-leverage visual lever for BokChoy's dev-exclusive audience. Visual-abstract + visual-product-mockup also rejected (audience-fit / UI-polish gates). **Primary failure mode:** SDK slug-resolution error semantics — typo `'gem'` instead of `'gems'` must produce actionable error naming available slugs, not generic 404. **4 mitigations** named (named exception classes, cache TTL + force-refresh, real-world starter slugs aligned to `[[cockpit/first-run-journey]]` step 6, hero CTA end-to-end verification). **8 cascade obligations** (SDK slug-resolution layer extends `[[oss-sdk-only]]` cascade; new `GET /v1/currencies` backend endpoint extends `[[cockpit/admin-list-endpoints-contract]]`; (marketing)/page.tsx + pricing.tsx + docs entry routes; top-nav component; first-run-journey step 6 amendment to seed default currencies; marketing-copy-authoring follow-on session; closes `[[marketing/landing-patterns-research]]` Op #1+#2+#3+#4 — Op #5 trust-signal substitute strategy remains open). **9 revisit-when triggers** named (5 dropped-route triggers + hero copy A/B threshold + V2 SDK-blowout fallback to V3 + IA depth on multi-SKU expansion + docs tech-stack sub-pick). Confidence: high.
- [[marketing/oss-core-marketing-research]] — type: research — **OSS-core commercial dev-tool marketing extension LANDED 2026-05-14.** Companion to `[[marketing/landing-patterns-research]]`; 3 sites surveyed (Supabase + PostHog + Plausible). **F8 LOAD-BEARING:** "OSS-core → OSS-prominent marketing" FALSIFIED by PostHog — has OSS-core repo but does NOT surface OSS on the homepage. Pattern that holds: OSS positioning prominence correlates with whether OSS IS the differentiator (Plausible: privacy+lock-in → OSS leads; PostHog: all-in-one platform → OSS hidden; Supabase: in-between → OSS bullet). **F8a (amendment to prior F6):** direct competitor naming is conditional — wins when (i) opponent has high brand recognition AND (ii) contrast is values-based not feature-based. htmx vs React ✓; Plausible vs Google Analytics ✓ (named in HEADLINE + dedicated /compare page with 4 sub-pages — most aggressive competitor framing in 9-cite survey). BokChoy has no qualifying opponent → "vs build-it-yourself" is the strongest viable frame, not "vs Lootlocker." **F9:** "mention self-host, don't lead with it" — universal across 3/3. Self-host is a lock-in escape hatch in the marketing, NOT an adoption path. Commercial-tier CTA is what the marketing actually pushes. **F10:** 3 trust-signal substitute patterns at OSS-core class — (i) scale numbers (PostHog "98% free, 1M events/mo"; Plausible "18k subs, 260B pageviews, 99.99% uptime"), (ii) named-individual testimonials (Plausible: DHH, Clem Delangue, John O'Nolan — vs customer-logo wall), (iii) values-positioning (Plausible's EU 🇪🇺 / no-cookies / "surveillance capitalism" framing). For BokChoy v1: scale numbers + named testimonials unavailable; **technical-trust signals viable substitute** — audit-log replayability, defense-in-depth surfacing, Postgres-native (no-NoSQL-lock-in). **F11:** brand voice register variation is wider in OSS-core class — PostHog ironic-dev-community (`home.mdx` nav items, "Switch to website mode" toggle, "Not endorsed by Kim K"), Plausible earnest-values, Supabase utilitarian. **C3:** prior F6 amended (numerical claim holds 7/9 but framing was too strong). **C4:** prior F2 framing wrong — "horizontality" replaced with "single-purpose vs platform" (Supabase falsifies horizontality; BokChoy's single-purpose wallet primitives still fit code-as-hero pattern under the corrected framing). **2 cascade amendments owed inline to `[[marketing/landing-patterns-research]]`** (F2 + F6 refinements). **4 operational implications added (numbered 8-11 continuing prior entry):** OSS-or-proprietary BokChoy decision surfaced (#8 — load-bearing prior question, not decidable from this research), values-vs-build-it-yourself differentiation framing (#9), technical-trust substitute strategy refined (#10), voice register explicit open thread (#11). **5 open threads** carried/added. Confidence: high (3/3 cites clean on the OSS-specific questions; Supabase partial extract on the shared questions but covered by prior entry's Open Thread #1).
- [[marketing/landing-patterns-research]] — type: research — **B2B dev-tool / dev-infra marketing-landing-page survey LANDED 2026-05-14.** Triggered by /design pickup of marketing feature; user halted *"don't restrict bokchoy at mvp, marketing can lie its okay it just need to show what it actually offers"* — research scope set to mature aesthetic register regardless of BokChoy's stage, with the honesty boundary *the API offered must reflect what the backend ships; polish/scale claims are aspirational*. 6 production sites surveyed: Stripe + Resend + Clerk + Lootlocker + RevenueCat + htmx (the last as contradiction probe — falsified the "everyone does the Stripe shape" mode-collapse). **F1 LOAD-BEARING:** hero composition is [noun-claim] + [outcome-frame], 6–12 words, universal across 6/6. Two viable shapes — infrastructure-as-noun + outcome (Stripe, Resend, Lootlocker) OR outcome-only (RevenueCat). **F2 LOAD-BEARING:** code-on-landing splits by product horizontality — Stripe/RevenueCat/Lootlocker/Clerk ship NO code on landing (sell to non-devs too); htmx ships code AS hero (dev-exclusive audience). Resend + Clerk code question unverified by WebFetch (open thread #1). BokChoy audience is dev-exclusive → code-as-hero pattern is on the table. **F3:** trust signals at maturity are universal triple — customer logos + scale numbers + compliance badges (SOC2/GDPR). BokChoy v1 has ZERO of the three. Substitute strategy owed: technical-trust signals (idempotency, audit-log, defense-in-depth proxy.ts gate) substituting for social-trust signals. **F4:** primary CTA = "Get started free / Sign up free" universal (5/6); Clerk's docs-first deviation is the alternative pattern. "Talk to sales" appears only as SECONDARY (RevenueCat) — never primary in the survey. **F5:** IA depth scales with product breadth, not company maturity. BokChoy v1 single-product single-persona → Clerk/Resend tight pattern (~5-6 nav items), NOT Stripe/RevenueCat depth. **F6:** direct competitor naming on landing is rare (1/6 — htmx vs React). 5/6 commercial dev-tools position-not-compare. /build-vs-buy/ page as follow-on (RevenueCat pattern) is the right shape, NOT a vs-table at v1. **F7:** above-fold visual splits 3 ways — abstract-decorative / product-mockup / code-as-hero. /design pick. **C1 unverified gap:** Resend + Clerk code-on-landing claim could not be extracted by WebFetch; training-data inference rejected; vaulted as open thread #1 (headless-browser pass owed before F7 pick). **6 open threads** named (code verification, voice survey separable, /customers-at-zero-customers shape, /build-vs-buy follow-on, sponsor-substitute pattern, API-call-readability tiebreaker). Confidence: medium (the 2/6 code-on-landing unverified gap downgrades from high). Operational implications: 7 numbered items the next /design pass attacks (hero copy, CTA target, hero visual, IA depth, trust-signal substitute strategy, differentiation framing, honesty-boundary enforcement).

## _shared

- [[_shared/comment-conventions]] — type: decision — **Comment conventions RESOLVED 2026-05-18.** Closes the design pass triggered by user observation that BokChoy comments are "a mess and bloating files" (file scan confirms: 19–39% density across 22 files vs ~4–5% cluster median per research). **(i) Density target:** standalone comment lines / total LOC <5% per file, outside JSDoc-on-exported-APIs (no cap on JSDoc-on-exports — Effect + Drizzle precedent). **(ii) File header policy:** ≤3-line `@module` JSDoc (hono precedent) OR 1-line class-level JSDoc (stripe-node precedent). Multi-paragraph narrative headers forbidden. **(iii) KEEP:** JSDoc-on-exports, 1–3 line invariant inline comments naming non-obvious constraints, external spec/RFC/docs URL citations, workaround citations with issue URLs. **(iv) `[[wikilink]]` discipline:** allowed only as 1-line JSDoc on exported declaration implementing a vault contract; cap 1/file; no restated content. **(v) STRIP:** narrative file headers, re-explains, span-attribute/auth-chain boilerplate repeated per handler, wikilink restatements, inline design-seat interpretation flags. **(vi) Advisory enforcement:** ship `scripts/check-comment-density.ts` — simple regex, threshold 10% (2× cluster median), advisory output not CI gate. **(vii) Cleanup-pass scope quantified:** 22 files identified, ~770 standalone comment lines surveyed, estimated 400–550 removable preserving keep-rules. /refactoring seat owns per-file pass. SDK files (`sdk-node/wallets.ts`/`errors.ts`) exempt — public API surface, density may rise via Effect/Drizzle-shape `@example` JSDoc in future SDK publish slice. **4 rejected alternatives** with named winning conditions: (D-narrative) keep narrative headers, (E-loose) pin density at 10% per financial-ledger framing, (S-no) skip advisory script, (W-c-only) inline-only wikilinks. **5 cascade obligations:** check-comment-density script + lint:comments root script + cleanup-pass /refactoring seat + bocek primitive amendment + future SDK publish JSDoc completeness pass. Confidence: high.
- [[_shared/.research/comment-conventions-research]] — type: research — **Comment conventions across TS-stack production codebases at BokChoy's scale-class LANDED 2026-05-18.** 7 source files surveyed across 5 named codebases (`stripe-node` × 2, `better-auth`, `hono` × 2, `drizzle-orm`, `trpc`) + 1 contradiction probe (Effect-TS). **F1 density (load-bearing):** cluster median is **~4–5% standalone comment density** outside JSDoc-on-exported-APIs (stripe-node 1.95%/4.8%, hono-base 4.5%, better-auth <1%, trpc 1.7%, drizzle 11.8% with all weight on JSDoc-with-examples). BokChoy's current state ~23% on new files like `balance-by-external-id.ts` = **~5× class median**. **F2 shape (load-bearing):** 3 earned archetypes: (c-export) JSDoc on exported APIs with `@param`/`@returns`/`@template`, drizzle adds runnable examples; (b) 1–3 line inline invariant naming a constraint a reader can't recover from code (trpc "Then is reserved word because Proxy semantics"; better-auth "Hash password to prevent timing attacks"; stripe-node "Capture caller stack trace before async boundary"); (d) external spec/RFC/docs URL citations (stripe-node cites RFC 7230 inline). Not earned: (a) re-explains what code does; multi-paragraph narrative file headers. **F3 file headers:** absent or ≤3 lines across 6 of 7 surveyed files (hono `@module` 3-line max; stripe-node 1-line class doc max). **BokChoy's 14-line narrative-headers match nothing in the cluster.** **F4 contradiction probe:** Effect-TS at 42% density surveyed; 95% of that is JSDoc-on-exports, only ~5% in-function inline. Audience-class mismatch (framework foundation vs SaaS SDK) — Effect does NOT support BokChoy's narrative-header pattern; if anything strengthens the JSDoc-on-exports-only rule. **F5 enforcement:** none of surveyed codebases use comment-density lint; discipline is review-mediated. Advisory script (`scripts/check-comment-density.ts`) is feasible but not industry-standard. **Operational implications for `[[_shared/comment-conventions]]` decision:** (1) density target <5% standalone outside JSDoc-on-exports; (2) file headers forbidden beyond 3-line `@module` or 1-line class JSDoc; (3) keep JSDoc-on-exports + (b) invariant inline + (d) spec URL citations; (4) strip narrative headers + `[[wikilink]]` restatements + (a) re-explains; (5) `[[wikilink]]` allowed as 1-line JSDoc analog to stripe-node's RFC URL citations — pin syntax in /design; (6) cleanup pass scope owed (per-file scan). Confidence: high. **4 open threads** (cleanup-pass scope estimate, advisory script decision, `[[wikilink]]`-in-JSDoc concrete syntax, JSDoc completeness on @bokchoy/sdk-node public methods). (cross-cutting)

- [[oss-sdk-only]] — type: decision — **OSS-SDK-only licensing model RESOLVED 2026-05-14.** BokChoy ships under mixed-source: client SDKs (Node, Unity, Unreal, Godot) + `@bokchoy/shared-types` are MIT-licensed, public per-SDK GitHub repos under `github.com/bokchoy`; backend (`@bokchoy/backend`, `@bokchoy/wallet`, `@bokchoy/db`, `@bokchoy/auth-config`) + cockpit (`@bokchoy/cockpit`) stay proprietary. **User defense (operational, NOT strategic):** *"good to get patches from people actually using the product and makes it easy to receive sdk updates"* — production-cited × 4 (Stripe/Resend/Clerk/RevenueCat all ship this exact shape per `[[marketing/landing-patterns-research]]`). DCO not CLA. SECURITY.md + security@bokchoy.com + 90-day responsible disclosure. **3 rejected alternatives with named winning conditions:** (A) full proprietary — wins on strategic optionality + private CVE; rejected on operational benefit loss + product-class community-fit. (B) full OSS backend — wins on F10 trust signal at scale + audit access + lock-in defang; rejected on brand-protection load (Elastic/Redis re-host risk) + weak product-class community-fit (game devs don't fork backend infra) + hostile close-later cost. (H) defer — wins when answer hasn't surfaced from customer signal; rejected because user engaged the question rather than deferring and Position C earned the vault on clear operational grounds. **Primary failure mode:** backend internals leak through `@bokchoy/shared-types` boundary (field names, error code enums, JSDoc hints). **6 mitigations** named (manual extraction discipline, pre-release SDK diff review, trademark + namespace claiming, CVE-disclosure runbook, DCO CI check, LICENSE-presence CI). **6 revisit-when triggers** named (≥3 prospects ask for backend source / compliance escrow demand / >50 SDK contributors / Apache-2.0-requiring customer / acquisition-or-partnership closing-SDKs scenario / compliance-sensitive feature class added). **8 cascade obligations** for implementation when SDKs land (trademark filing, namespace claiming, `@bokchoy/shared-types` extraction, SECURITY.md template, DCO action, LICENSE template, SDK pre-release review runbook, marketing-research operational-implication-#8 RESOLVED). Closes `[[marketing/landing-patterns-research]]` Op #8 + `[[marketing/oss-core-marketing-research]]` Open Thread #2. Confidence: high.
- [[direct-mutation-lint-opt-out-shape]] — type: decision — **Direct-mutation lint opt-out shape DECISION VAULTED 2026-05-10 as `(β) N=1 lookback + dual `//`/`--` recognition`** per `[[direct-mutation-lint-opt-out-research]]`. `scripts/check-direct-wallet-mutation.ts` accepts `// allow-direct-mutation: <reason>` (TS context) on same-line OR line-above OR `-- allow-direct-mutation: <reason>` (Postgres SQL comment) on the line immediately preceding the SQL keyword inside `sql\`...\`` template literals. **Per-statement granularity preserved** — `[[reaper-schedule-deferral]]` line 57's contract that the per-statement opt-out PR is the trigger event for slice 6.5 stays intact. **(α) range markers REJECTED** on granularity-loss attack: silent additions inside an existing range bypass the per-statement audit signal. **(γ) rewrite-to-Drizzle-query-builder REJECTED** on generality: lint mechanism should be agnostic to SQL idiom; future cross-cutting middlewares (audit log, outbox, DSR queue) will hit the same shape regardless. **(a) K=2 lookback FALSIFIED** by F1 — no production lint tool ships K>1 lookback as a primary directive. **(d) per-file EXCLUDE_PREFIXES REJECTED EARLIER** for contradicting `[[reaper-schedule-deferral]]`. Production-cited × 3 (Biome / ESLint / TS for N=1 next-line) + docs-cited × 1 (Postgres `--` SQL comment grammar). Implementation contract: ~3-line script change (regex alternation + N=1 lookback function); on-disk middleware opt-out comments repositioned from outside the template to inside as `--` Postgres comments on the line immediately above each SQL keyword; failure-message tail updated to name both placement options. F2 secondary question (rewrite middleware to query builder) stays separable. Revisit-when: future region-level cross-cutting middleware compound the per-statement annotation cost (re-evaluate α as complementary mode); Biome plugin maturity makes AST-aware rule migration viable; `[[reaper-schedule-deferral]]` trigger fires + slice 6.5 schedules.
- [[direct-mutation-lint-opt-out-research]] — type: research — **Lint opt-out shape for multi-line `sql\`...\`` templates RESEARCH (2026-05-10)** — closes /design pushback on `[[gaps]]` GAP 10. /design seat picked **(a) K=2 lookback** citing "TS-ecosystem convention" — research **falsifies** this. 9 sources triangulated. **F1 (LOAD-BEARING): TS-ecosystem suppression conventions are N=1 next-line OR explicit range markers.** Production-cited × 3 (Biome 2.x next-line + range markers `biome-ignore-start`/`biome-ignore-end`; ESLint `eslint-disable-next-line` + `/* eslint-disable */` ... `/* eslint-enable */` blocks; TS `// @ts-expect-error` N=1 only). **No production lint tool ships K>1 lookback as a primary directive.** ESLint custom-rule API allows building K>1 (via `sourceCode.getCommentsBefore` / `sourceCode.lines[]`) but no published plugin or stock directive surfaces it. **F2:** Drizzle production INSERT/UPDATE/DELETE goes through query builder (`db.insert(table).values()`) not multi-line `sql\`...\``. Better Auth `sql\`` usage is overwhelmingly inline single-line; multi-line `sql\`SELECT\`` only in test code; `gh search code 'sql\`\\nINSERT INTO' --extension ts` returned zero matches across public TS. **F3:** No off-the-shelf prior art for "RLS-protected-table direct-mutation lint" — `eslint-plugin-immutable`/`-functional`/`-better-mutation` target JS variable mutation; `pgrls` lints Postgres RLS policies at the SQL layer, not TS source. BokChoy's `check-direct-wallet-mutation.ts` is bespoke; conventions inherited from Biome/ESLint, not from a domain-specific plugin. **Operational implication for /design pushback:** two viable shapes survive — **(α) range markers** `// allow-direct-mutation-start: <reason>` ... `// allow-direct-mutation-end` (production-cited × 2, region granularity, +2 lines per single statement); **(β) N=1 lookback + accept `--` SQL comments** alongside `//` (industry-cited × 3 for N=1, `--` lives inside template as Postgres comment on line preceding SQL keyword, per-statement granularity, mixes TS+SQL comment styles). **K=2 lookback DROPPED from candidate set** — fails *Production-grade default* industry-standard gate. Secondary design question surfaced (F2 conflict): should the on-disk middleware use Drizzle query builder instead of raw `sql\`INSERT INTO\`` — separable from lint-mechanism pick.
- [[url-pattern-research]] — type: research — **URL pattern for verb-heavy state-transition actions on identified resources RESEARCH (2026-05-09)** — closes the /design self-attack on G1 from `[[gaps]]` GAP 9. /design (a) flat-action `POST /v1/wallet/credit` was based on a misread of Stripe; mitigation-by-assertion failed under triangulation. **6 sources confirm a fourth pattern (d) I missed:** `POST /v1/{resource-plural}/{id}/{verb}` slash-suffix-verb. Production-cited × 2 ecosystems (Stripe + GitHub), 10+ named endpoints between them. **Stripe shipped `POST /v1/charges/{id}/capture`, `/refund`, `/cancel`, `/confirm`, `/attach`, `/void` — NOT `POST /v1/charges/capture`** (no id). GitHub ships same shape: `PUT /repos/{owner}/{repo}/pulls/{number}/merge`, `/archive`, `/update-branch`, `/lock`. Google AIP-136 ships colon-variant `:verb` — same intent, cosmetic difference. Cal.com (`/v2/bookings/get-all-bookings`) and Better Auth (`/api/auth/sign-in/email`) ship verb-slug-as-segment but at *collection-level* or *auth-method-level*, NOT resource-id-scoped — different problem class from BokChoy's `walletCredit(walletId, ...)`. **Industry "nouns only" prescription FALSIFIED at production by Stripe + GitHub** (per Contradiction protocol production code wins). **F2 the /design lock-out concern doesn't materialize:** under pattern (d), `GET /v1/wallets/{walletId}` (balance read) and `POST /v1/wallets/{walletId}/credit` (action) share resource prefix, no hybrid coexistence problem. **For BokChoy:** `POST /v1/wallets/{walletId}/credit`, `/debit`; `POST /v1/players/{playerId}/deidentify`; `POST /v1/projects/{projectId}/bootstrap-reason-codes`. Plural collection naming production-cited × 2 (Stripe + GitHub). Multi-word verbs hyphenated kebab-case (GitHub `/update-branch`). `POST` over `PUT` matches non-idempotent-at-HTTP-layer semantics (idempotency is server-derived per `[[idempotency-strategy]]`). **Body field casing orthogonal — `[[http-contract-research]]` F1 still stands**; /design picks snake vs camel separately. Open threads: full Stripe API surface enumeration (marginal value), Google AIP weight in counting, multi-word verb conventions cross-API.
- [[otel-stack-research]] — type: research — **OTel SDK + exporter pick for Bun + Hono + postgres-js RESEARCH (2026-05-09)** — Entry 2 of 2 queued behind GAP 9 (slice 8.1 G7). 12 sources triangulated. **F1 (LOAD-BEARING): Bun + auto-instrumentation is structurally broken since 2023** — issue #3775 open 2.5yr, #26536 (2026-01-28) closed as duplicate, #13165 documents shimmer-patching breakage at Bun's bundle layer. #28968 (2026-04-07 open) proposes Bun-native OTel; not shipped. Deno 2.2 shipped native OTel Feb 2025; Bun unmatched. Discussion #7185: Jarred-Sumner Nov 2023 "Maybe" — no roadmap. **Workable path is manual instrumentation: `@opentelemetry/api` + `@opentelemetry/sdk-node` (or `sdk-trace-node` per Sentry) + `@opentelemetry/exporter-trace-otlp-http` + `@hono/otel` Hono-middleware (NOT `@opentelemetry/auto-instrumentations-node`).** F2 (Hono): `@hono/otel` is first-class honojs-org middleware (published 2026-03-04, authored Joakim Lorentz + Hong Minhee), uses `@opentelemetry/api` directly via `createMiddleware` from `hono/factory` — no shimmer monkey-patching, sidesteps Bun breakage. Captures `ATTR_HTTP_REQUEST_METHOD`, `ATTR_URL_FULL`, `ATTR_HTTP_ROUTE`, `ATTR_HTTP_RESPONSE_STATUS_CODE` + configurable headers. Limitation: instruments request-lifecycle, not per-middleware. F3 (postgres-js): **official `@opentelemetry/instrumentation-pg` does NOT cover postgres-js** (porsager/postgres) — covers `pg` only. Two community options (`wataruoguchi/otel-instrumentation-postgres` v1.0.0 2025-07-13 + `tomsanbear/opentelemetry-instrumentation-postgres`) both tier-3, single-maintainer, Bun-compat untested. **Recommended path: manual span emission inside `@bokchoy/wallet` wrappers** at the existing `// TODO(otel)` markers — tier-1 BokChoy code, sidesteps community-wrapper Bun question entirely. F4 (exporter): **Honeycomb 20M events/month free tier** is concrete and sufficient at indie tier (BokChoy peak ~7.7M/month, well within); Studio+ scale (~1.5B/month) requires Pro+. Grafana Cloud OTLP-native free tier exists; specific limits not pulled (open thread). Self-hosted collector premature for indie. Console exporter for dev. **Conflict 1 (NOT artificially resolved):** "OTel works on Bun" (Datadog/OneUptime vendor docs) vs "OTel doesn't work on Bun" (#3775/#26536) — both true at different layers. Manual instrumentation works; auto-instrumentation broken. **Conflict 2:** Sentry uses `sdk-trace-node` + `NodeTracerProvider`; OneUptime uses `sdk-node` + `NodeSDK` — practical equivalence, sdk-node bundles auto-init helpers (the broken-on-Bun part). Cascade: this is the same risk class as `[[backend-stack]]` F-Bun-1/2/3/4 — manual-instrumentation tax is part of the Bun-acceptance trade. **Sub-decisions for /design:** OTel-or-defer-one-more-slice, bootstrap location (`apps/backend/src/telemetry.ts` vs `@bokchoy/telemetry/`), Honeycomb vs Grafana Cloud, span-attribute spec. Open threads: community postgres-js wrapper Bun-compat verification, Grafana Cloud free-tier limit numbers, Bun #28968 ship signal, Trigger.dev's actual OTel-on-Bun shape.
- [[tooling]] — type: decision — **tooling pick RESOLVED 2026-05-04 per `[[tooling-research]]`.** Biome only for lint + format; no ESLint, no Prettier. Production-cited via biomejs.dev primary listing 15 tier-1 orgs (AWS, Google, Microsoft, Cloudflare, Coinbase, Vercel, Discord, Slack, etc.). Cascade enforcement: `[[frontend-stack]]` cross-runtime via Biome built-in `noRestrictedImports`; `[[backend-stack]]` cascade-10 via Bash/TS scripted-check (default; GritQL plugin alternative pending verification); `[[backend-stack]]` F1 amended to two-layer defense (RLS-fail-closed + type-level discipline + integration tests) — lint NOT load-bearing per production-cited Drizzle+Nile / pgvpd / Drizzle Discussion #1539. 5 failure modes (GritQL underperforms, Biome major-breakage, VSCode import-mangle, GraphQL formatter absent, plugin distribution out-of-scope). 3 rejected alternatives (ESLint+Prettier, hybrid Biome+scripts as separate, OXC). 7 cascade obligations queued for implementation (biome.json, deps + scripts, scripted-check, noRestrictedImports config, GritQL verify, F1 amendment, CI scaffold).
- [[tooling-research]] — type: research — answers six questions blocking sub-unit (4) tooling pick (Biome vs ESLint+Prettier vs hybrid). **Q1 (Biome AST capability):** v2 plugin system uses GritQL, diagnostic-only, first-iteration; can match `postgres()` calls + property absence for `[[backend-stack]]` cascade-10, less ergonomic than ESLint's mature plugins; Bash/TS scripted check is unblocking fallback (cf. cascade-9 FORCE-RLS pattern). **Q2 (`noRestrictedImports` cross-runtime):** built-in supports gitignore-style globs + bare imports — `bun` and `bun:*` patterns covered for `[[frontend-stack]]` cross-runtime discipline. **Q3 (F1 production patterns):** OVER-PROMISED in `[[backend-stack]]` — production teams (Drizzle+Nile AsyncLocalStorage, pgvpd connection-proxy, Drizzle Discussion #1539 unanswered) enforce tenant tx-wrapper at connection-layer (RLS + SET LOCAL fail-closed), NOT at lint layer. Drizzle's own ESLint plugin scope is `delete WHERE` only, not tx-wrapper. F1 mitigation should be amended to type-level + integration tests. **Q4 (Biome maturity 2026-Q2):** v2 ~14 months old, automated `biome migrate`, regular minors; breaking changes comparable in class to ESLint v9 flat-config; plugin system "first iteration" but adequate for BokChoy's load-bearing rules. **Q5 (production cites):** verified at primary `biomejs.dev` homepage — 15 named tier-1 orgs (AWS, Google, Microsoft, Cloudflare, Coinbase, Vercel, Discord, Slack, Astro, n8n, Node.js, Uniswap, Comcast, Canonical, Socket). Updates `[[bun-workspaces-research]]`'s tier-4 industry-consensus quote with tier-1 production-cited backing. **Q6 (contradiction probe — Biome→ESLint migration):** zero documented in surveyed scope (dev.to, Medium, GitHub, codemod, kittygiraudel.com). 10 sources triangulated. **Operational implications for `/design` tooling vault:** Position A (Biome only) is defendable on tier-1 evidence; F1 cascade needs amendment regardless of linter pick (type-level + tests, not lint); cascade-10 implementable via GritQL plugin OR Bash check; cross-runtime cascade by built-in `noRestrictedImports`. Friction known: import-sorting VSCode quirks + no GraphQL formatter.

- [[research-scope]] — type: decision — sequencing: landscape survey first, path-A deep dive on chosen slice
- [[landscape-survey]] — type: research — competitor surface, economy shapes, who-pays grid; teed up slice decision
- [[africa-field-notes]] — type: research — Africa-as-primary-market wedge killed by field data
- [[design-claims-register]] — type: discovery — audit of `docs/DESIGN.md` v0.1.0; 64 numbered claims, ranked P0/P1/P2/P3 + Park list
- [[cockpit-gap-research]] — type: research — resolves CL-007; "designer-first live-ops UX gap" not category-creating; 6+ of 9 cockpit dimensions shipped first-class by ≥3 named incumbents
- [[slice-cell-research]] — type: research — resolves CL-001 (partial); per-cell winning conditions for A1/B1/C1/D1; surfaces Heroic Labs Hiro as A1 competitor and damages partnership thesis
- [[sales-cycle-research]] — type: research — resolves CL-009 (falsified); §4's "12–18 month replacement cycle" is enterprise-tier data applied to mid-market product; mid-market benchmark is 30–120 days
- [[structural-moat-research]] — type: research — resolves CL-002 (falsified); reference frames don't translate; BokChoy fits GitHub/Dependabot encroachment pattern, not Stripe; AccelByte is the gaming-specific moat shape (founder credibility)
- [[integration-feasibility-research]] — type: research — resolves CL-006 (partial); 30-min figure is auth-bridge only (full integration is 1–3 days); Heroic partnership claim directly contradicted by Hiro; SDK deprecation risk concrete

## wedge

- [[wedge-decision]] — type: decision — keystone for DESIGN.md rewrite. Path 3 + 3-B + β MVP + free-tier cockpit + engine-first-with-AI + 20-month runway cap. Survived pressure-test of 5 research entries + AI-slop critique.
- [[mobile-f2p-economy-math-research]] — type: research — domain depth across 5 topics (gacha PMF, retention curves, sink/faucet, LTV archetype, battle pass). Cascades into 8 backend primitive requirements for MVP feature list.
- [[indie-smb-pricing-research]] — type: research — resolves CL-010 (partial). Rebuilt tier mix: Free $0 / Indie $99 / Studio $499 / Studio+ $1,499 / Enterprise. Game-economy incumbents lack permanent free tier with cockpit — confirmed wedge mechanism.

## mvp

- [[mvp-feature-sequence]] — type: decision — 7-month linear MVP plan: usable spine (months 1–3), cockpit core (4–5), live-ops mechanics (6), AI tooling (7). Each milestone is a usable product. AI cuts first on slip.

## wallet

- [[wallet/balance-history-contract]] — type: decision — **Player-centric balance + history read contract RESOLVED 2026-05-18.** Closes GAP 11 (11 sub-decisions: 2 URL shapes, unknown-player-on-read, pagination, filters, balance response shape, history per-row shape, OTel attrs, idempotency on GET, read auth, wrapper layer). **(i)** `GET /v1/players/{playerExternalId}/wallets/{currencyCode}` (balance — REST resource read) + `GET /v1/players/{playerExternalId}/wallets/{currencyCode}/transactions` (history — Stripe-shape nested cursor-paginated, default limit 10 / max 100, ORDER BY id DESC). **(ii)** Auth chain inherits M-1.5: `apiKeyMiddleware` + `sValidator('param')` + `sValidator('query')` for history; idempotencyMiddleware NOT mounted on GETs per Stripe production convention. **(iii) STATELESS READS** — unknown player → 200 synthesized zero/empty, NO row materialization, preserving M-1.5 PII discipline. `UnknownPlayerError` stays reserved for future explicit-create flow. User-ratified customer-shape: "new player → balance 0 is the common case." **(iv) Per-row `balanceAfter` OMITTED** per `[[wallet/.research/per-row-running-balance-research]]` F1 — production-cited × 3 (Stripe BalanceTransaction, PlayFab Economy v2 Transaction, Nakama walletLedger Go struct) at game-economy class explicit reject; F2 financial-ledger class (Modern Treasury, Oracle Fusion) ships per-row balance but is cross-class for BokChoy's positioning. `transactions.balance_after` column NOT added; schema unchanged from M-1.5. **(v) Wrappers** in `packages/wallet/`: `walletBalanceByExternalId` + `walletHistoryByExternalId`, plain Drizzle RLS-scoped reads (no stored functions — atomicity not required), discriminated-union return `{exists: false} | {exists: true, ...}`. **(vi) Error envelope** — VALIDATION_ERROR only; no 404 class (reads return synthesized zero on missing wallet). **(vii) OTel** — spans `wallet.balance` / `wallet.history`, attrs inherit M-1.5 HMAC-player_external_id_hash + new `wallet_exists` / `transaction_count` / `has_more` / `cursor` / `limit` diagnostics. **19 cascade obligations for /implementation:** 2 new wrapper files + 2 new GET handlers in `apps/backend/src/wallet/index.ts` + 2 new Zod schemas + SDK `balance()` + `history()` methods + new HTTP client `get()` method + `code-walkthrough.tsx` snippet update (move balance + history from "Coming soon" to "Today" with live shapes) + smoke-script extension + small amendment to `[[wallet/wallet-http-contract]]` registering the 2 new GETs + `[[marketing/v1-shape]]` Cascade #10 post-impl amendment. **5 rejected alternatives** with named winning conditions: (α) persist column (financial-ledger class pivot trigger), (γ) window-function compute (dominated), (b) flat per-player history (future non-breaking-additive endpoint when N currencies large), (c) top-level collection (cockpit Month 6 audit slice), G11.3 lazy-create-on-read (PII discipline regression), G11.3 404 UnknownPlayerError (inconsistent with M-1.5 lazy-create-on-credit asymmetry). **2 failure modes with mitigations** (pagination cursor poisoning; RLS GUC missed-context). **6 revisit-when triggers.** /implementation seat resumes with this entry as the contract to quote verbatim. Confidence: high.
- [[wallet/credit-route-contract]] — type: decision — **Slice M-1.5 contract RESOLVED 2026-05-14.** Four sub-decisions ratified after research-grounded 4-question fork: **(i) Route shape = B2** (player-centric URL `POST /v1/players/:playerExternalId/wallets/:currencyCode/credit` + `/debit`). Auth via `apiKeyMiddleware`, idempotency via existing middleware, body shape mirrors existing `creditDebitBody` minus currencyId (currency now in URL). Response 201 bare-data; errors Stripe-wrapped including new 404 `UNKNOWN_CURRENCY` with `availableCodes: string[]`. OTel span `wallet.credit_by_external_id` with `bokchoy.player_external_id_hash` (HMAC-SHA256 truncated to 16 hex). **(ii) Schema: `players.external_id text` nullable** + CHECK regex `^[A-Za-z0-9._-]{1,128}$` (URL-safe-by-construction) + unique index `(project_id, external_id) WHERE external_id IS NOT NULL`. Case-sensitive. Existing auth-flow rows keep NULL. **(iii) Lazy-create-player semantics:** INSERT...ON CONFLICT DO NOTHING for both players and wallets rows on first credit. Race-safe. DoS-via-typo mitigated via per-API-key rate limit (1000 req/min default suggestion) + player-count observability alert. Schema-comment at `wallet.ts:92` DELETED (claimed lazy-create lived in `wallet_credit`; now lives in new player-centric SQL function `wallet_credit_by_external_id`). **(iv) PII handling: Stripe-metadata pattern** (customer-responsibility framing in SECURITY.md) plus defense-in-depth tweak: OTel hashes external_id with deployment-scoped salt for span correlation; customer-facing error responses keep raw value for debug-ability. **5 rejected alternatives** with named winning conditions (B1 two-call upsert, B3 body-encoded, B4 explicit create, reject-with-UnknownPlayerError, full-hash-everywhere PII). **3 failure modes** named (DoS-via-typo, PII-via-external_id, schema-comment drift third instance). **5 mitigations**. **13 cascade obligations** for M-1.5 implementation (migration + Drizzle column + new SQL function + wrapper + 2 new route handlers + Zod schemas + OTel hash + env var + v1-shape Cascade #1(a) supersession + SDK package revision + SECURITY.md doc + smoke-script extension). **`[[marketing/v1-shape]]` Cascade #1(a) SUPERSEDED** — SDK slug-resolver no longer needed; package estimate drops from ~500 LOC to ~250-350. **Unblocks M-4** once M-1.5 ships. Confidence: high.
- [[wallet/.research/per-row-running-balance-research]] — type: research — **Does game-economy class ship per-row running balance? LANDED 2026-05-18.** Answers GAP 11 G11.7 (persist `transactions.balance_after` α vs omit β). 8 sources triangulated across two problem classes. **F1 game-economy class converges on OMIT** — production-cited × 3 explicit (Stripe `BalanceTransaction` no `ending_balance` per tier-2 docs walk; PlayFab Economy v2 `Transaction` no balance-after per tier-2 REST API spec; Nakama `walletLedger` Go struct `{ID, UserID, Changeset, Metadata, CreateTime, UpdateTime}` per tier-1 production code at `heroiclabs/nakama/server/core_wallet.go` master) plus 1 by-omission (RevenueCat doesn't ship transaction-history endpoint at all per Developer API v2 docs). **F2 financial-ledger / enterprise-accounting class converges on SHIP** — Modern Treasury `resulting_ledger_account_balances` (tier-4 vendor blog) + Oracle Fusion `EndingBalance` (tier-2 docs). **F3 audience-class verdict** places BokChoy with F1 per `[[marketing/v1-shape]]` "wallet infrastructure for game economies" + `[[marketing/currencies-endpoint-research]]` F4 audience-scale-match (indie/SMB F2P, RevenueCat + LootLocker tier). Conflict NOT artificially resolved — both findings stand; audience-class match decides. **Operational implication:** /design picks (β) — omit `balanceAfter` from history rows; do NOT add `transactions.balance_after` column; customers compute running totals client-side from `amount` + `kind` signed delta. /design seat's (α) defense leaned on three uncited claims that all fell: "Stripe ships ending_balance per row" FALSE, "ledgers ship running balance" TRUE only at F2 class, "$1/month Railway pricing" not load-bearing. **Supporting findings:** Q4 NUMERIC(20,4) storage = 13-18 bytes variable per Postgres 16 docs (within /design seat's 16-byte estimate); Q3 ADD COLUMN NULL is metadata-only on regular tables (PG11+ optimization) — partitioned-table behavior strong-inferred but not directly cited (open thread). Confidence: high. **3 open threads:** Postgres ADD COLUMN cost on partitioned tables direct cite owed; LootLocker + AccelByte schemas opaque in public docs; SDK docs owe customer-side running-total compute pattern.
- [[wallet/credit-route-shape-research]] — type: research — **Production-convention survey for player→walletId resolution + credit wire shape LANDED 2026-05-14.** Triggered by /implementation seat hitting the M-4 blocker: SDK API per `[[marketing/v1-shape]]` (iii) is `wallets.credit({ player, currency, ... })` but backend's `POST /v1/wallets/:walletId/credit` requires a known walletId; no resolution path exists; `wallet_credit` SQL function raises `WalletNotFound` despite the schema comment at `packages/db/src/schema/wallet.ts:92` claiming lazy-create. 7 sources surveyed across game-economy SDK class. **F1 LOAD-BEARING:** game-economy SDK class converges on **player-centric, single-call** credit routes. 3 tier-2 production cites (PlayFab `AddInventoryItems` with body-encoded Entity; Nakama `walletUpdate(userId, map)`; RevenueCat `POST /v2/projects/{p}/customers/{c}/virtual_currencies/{cc}/actions/update_balance` URL-encoded customer + currency). Plus tier-4 corroboration (LootLocker + AccelByte player-centric inferred). **F2:** Implicit lazy-create-on-first-credit is the production default — no surveyed SDK requires a separate wallet-registration call. Schema-comment-vs-SQL-function drift confirmed. **F3:** URL-vs-body axis for player encoding is split, 3-of-5 URL-encoded. **F4:** Idempotency-key universal. **F5:** Server-side-only authority universal (BokChoy's `apiKeyMiddleware` matches). **Contradiction probe** (walletId-in-URL-path for game-economy class) returned ZERO hits — pattern appears only in crypto / fintech (wrong problem class — wallet IS user's primary identity). **Operational implications:** B1 + B4 (two-call patterns) **REJECTED on production-convention grounds**. B2 (player-centric URL route) and B3 (body-encoded sibling route + wallet_credit lazy-create) are the remaining candidates. **B2 has stronger production-convention support + smaller SDK package surface** (slug-resolver becomes unnecessary because backend handles slug→UUID server-side). **B3 has stronger schema-author-intent alignment** (the schema comment was promising B3-shaped lazy-create all along). /design picks; either way M-1.5 (new backend endpoint + SQL function modification) ships before M-4 (SDK package). 4 open threads (LootLocker schema tier-2, AccelByte schema tier-2, B2 vs B3 decision, currency-in-URL slug-vs-UUID encoding). Confidence: high.
- [[wallet-http-contract]] — type: decision — **Wallet HTTP contract RESOLVED 2026-05-09. Closes GAP 9 (8 sub-decisions).** Slice 8.1 splits into three sub-slices for snapshot tracking: **8.1a** (auth + RLS type) ships `api_keys` table + Bearer middleware + `bk_<env>_<random>` key format + key-creation CLI + `TenantTx` branded type closing `[[backend-stack]]` F1 mitigation #2 open thread + smoke `GET /v1/health-authed`. **8.1b** (telemetry + idempotency middleware) ships `apps/backend/src/telemetry.ts` (NodeSDK + OTLP/HTTP + Honeycomb endpoint via env var; Console exporter for dev; AlwaysOn sampler; W3C TraceContext) + `@hono/otel` middleware + Idempotency-Key middleware per `[[idempotency-strategy]]` D2-α (NULL→INSERT-with-locked_at→run→UPDATE; replay if completed+body-matches; 409 BC001 if locked+incomplete; 422 BC002 if completed+body-mismatch; 30s lock timeout; trips `[[reaper-schedule-deferral]]` reopen trigger — schedule pg_cron reaper here or 8.1.5 follow-on). **8.1c** (wallet handler) ships `POST /v1/wallets/{walletId}/credit` + `/debit` with `@hono/standard-validator` + Zod 4.x schema + Hono error middleware translating `WalletError`→Stripe-wrapped JSON + manual `tracer.startActiveSpan` inside wrappers replacing `// TODO(otel)` markers + 13-item smoke-test list. **URL pattern:** `POST /v1/{resource-plural}/{id}/{verb}` slash-suffix-verb (production-cited × 2 via `[[url-pattern-research]]`). Plural collection. Multi-word verbs kebab-case. POST not PUT (non-idempotent at HTTP — server-derived idempotency). `/v1/` stable indefinitely (Stripe ~10yr); `Bokchoy-Version: YYYY-MM-DD` header reserved for date-granularity breaking changes. **Body casing:** camelCase (TS-native, customer cluster Unity C# + Unreal C++ + TS all camel-friendly). **Response shape:** top-level resource (`{id,walletId,status}`), no data wrapper. **Error envelope:** Stripe-wrapped `{error:{code,message,...flattened-details}}` (production-cited × 2: Stripe + Cal.com) — defended on customer-SDK-language asymmetry: future polyglot SDK preserved. BCxxx→HTTP map verbatim from `[[wallet-mechanics]]` §SQLSTATE; `409→idempotency_error` direct Stripe precedent for BC001. **Validator:** Standard Schema interop (`@hono/standard-validator` + Zod 4.x; switchable to Valibot/ArkType later). **Auth:** SDK API key (`Authorization: Bearer <key>`) validated against `api_keys` row; `projectId` derives from validated row, NOT from header/path. `walletDeidentifyPlayer`/`bootstrapProjectReasonCodes` handlers deferred to post-8.1 (different auth surfaces). **OTel stack:** manual instrumentation per `[[otel-stack-research]]` F1 (Bun shimmer-patching broken; `auto-instrumentations-node` NOT installed); `@hono/otel` middleware + manual `tracer.startActiveSpan` inside wrappers; Honeycomb 20M events/month free tier sufficient at indie peak ~7.7M/month. **Rejected alternatives** with named winning conditions: (a) flat-action `POST /v1/wallet/credit` (production-falsified by source-walk), (b) REST nested `/wallets/{id}/credits` (loses 1:1 wrapper mapping), (c) Cal.com verb-slug at collection (different problem class — collection-level), (Y) flat-top-level error body (TS-only and consistency-with-Better-Auth weight; closes polyglot SDK path), (Z) RFC 9457 problem+json (spec-only, no major TS-native production adopter). **Failure modes:** F1 API-key leak (mitigated: secret-scanning recognizable prefix + per-project revocable + rate-limited); F2 idempotency lock-incomplete (30s timeout + SDK auto-retry on 409); F3 OTel exporter unreachable (observability-not-audit; transactions table is audit-of-record); F4 smoke-test corner-case miss (13-item checklist parsed by CI); F5 `Bokchoy-Version` header undesigned (revisit-when trigger). **Cascade:** updates `[[gaps]]` GAP 9 RFC 7807 cite to RFC 9457; closes `[[backend-stack]]` F1 mitigation #2 open thread on TenantTx; activates `[[reaper-schedule-deferral]]` reopen via slice 8.1b idempotency_keys writes.
- [[http-contract-research]] — type: research — **Wallet HTTP contract surface RESEARCH (2026-05-09)** — triangulated source-walk for slice 8.1 / GAP 9 (route shape + validator + error body). 13 sources: Stripe SDK + API docs (tier 1+2), Better Auth + better-call source (tier 1, BokChoy's auth lib), Cal.com API v2 (tier 2), Hono validation + HTTPException docs (tier 2), tRPC error-handling docs (tier 2 contradiction probe), PlayFab Economy v2 `Inventory/AddInventoryItems` (tier 2 F2P-domain), LootLocker partial (tier 4), RFC 9457 (tier 2 IETF spec, **obsoletes RFC 7807** July 2023), Standard Schema v1.1.0 spec (tier 2, jointly authored by Zod/Valibot/ArkType maintainers), RFC 9457 production-adoption probe (tier 4 negative result). **F1 (route): TS-native B2B SaaS REST `/v1/{resource}` (Stripe + Cal.com + Better Auth, 3 ecosystems) vs F2P RPC `/{Service}/{ActionName}` (PlayFab + LootLocker, 2 F2P-domain). BokChoy's wallet wrappers are verb-heavy (credit/debit/deidentify/bootstrap) — structurally closer to F2P RPC than CRUD REST**. F2 (validator): Standard Schema v1 stable (3.5k stars, 13 releases); Hono ships first-class `@hono/standard-validator` AND `@hono/zod-validator` from honojs org, neither labeled "official"; better-call already speaks `StandardSchemaV1.Issue` natively → validator-neutrality available without ecosystem cost. F3 (error body): genuine split — **Stripe-wrapped `{error:{code,message,...}}` (Stripe + Cal.com, 2 ecosystems) vs flat top-level `{code,message,...}` (Better Auth + better-call, 1 ecosystem but the lib BokChoy uses)**. RFC 9457 problem+json: spec-cited only — adoption probe surfaced no major TS-native production API. tRPC JSON-RPC framing rules out (different protocol). PlayFab `ApiErrorWrapper` 6-field verbose wrapper isn't widely emulated outside Microsoft. **Conflict resolution NOT artificially resolved** — /design picks (X) Stripe-wrapped vs (Y) flat-top-level based on whether SDK aspiration warrants Stripe-shape ergonomics or whether internal-developer ergonomic flat-shape (consistent with Better Auth's own emission) wins. Body casing: snake (Stripe) vs camel (TS-native default, Better Auth/Hono) vs Pascal (PlayFab Microsoft-stack) — three patterns, choice tracks SDK target language. **`409 → idempotency_error` (Stripe) is direct precedent for BokChoy's `BC001 → 409`**. Cascade: gap report's RFC 7807 cite needs correction to RFC 9457. Open threads: validator-version Standard-Schema support enumeration, Hono OpenAPI cascade for SDK/docs slices, F2P wallet-credit-equivalent (PlayFab Economy v2) re-fetch for direct domain comparison, LootLocker error body second-source.
- [[wrapper-shape]] — type: decision — **`@bokchoy/wallet` wrapper shape RESOLVED 2026-05-08.** Three forks closed in one entry: (1) parameter shape — db-separate + params-object signature (`walletCredit(db, { projectId, walletId, amount, ... })`), production-cited via Drizzle's `db.transaction(fn)` + tRPC's `({ ctx, input }) => ...` pattern + `idioms/typescript.md` line 65; (2) `WalletError` shape — single class with `code: BcCode` discriminant + per-code `details` discriminated union, production-cited × 4 via tRPC `TRPCError` + Hono `HTTPException` + Better Auth `APIError` + postgres-js `PostgresError` (TS-native ecosystem convention at framework-defining level); (3) FK-violation 23503 dispatch — translate at wrapper boundary, re-raise raw `PostgresError` for unknown constraint names. **Pinned non-forks:** `bigint→number` per schema's `mode: 'number'`; `amount: number` (JS-safe to 9×10^15, revisit on real-money); `metadata: Record<string, unknown>` per `[[wallet-mechanics]]` §3; Drizzle's `sql` template through `Db | Tx`; idempotency-replay invisible per `[[idempotency-strategy]]` D2-α. **`BcCode` union (10 codes)** — BC001/BC002 reserved for HTTP middleware (slice 4.6), BC010/020/021/022/040 wrapper-raised by today's SQL functions, BC030/050/060 reserved (BC050 active via 23503 translation on `transactions_project_reason_code_fk`). **`sqlstateToError(code, message, constraintName)` pure function** — returns `WalletError` for BC* and known 23503 constraint names; returns `null` (re-raise) for unknown 23503 / non-BC SQLSTATEs. **OTel span deferred** — wrap each call per `[[wallet-mechanics]]` Amendment Part 1 A3 layer 1; SDK choice not pinned, ship wrapper without span emission + `// TODO(otel)` until consumer arrives. **Rejected alternatives:** (A) positional args fails idioms/typescript.md line 65; (C) single-object loses Drizzle context-vs-data distinction; (Y) per-code subclass hierarchy wins for public SDK (Stripe/AWS pattern), revisit when `@bokchoy/wallet` flips `"private": false`; (Z) hybrid base+selective subclasses has arbitrary cut-line; (Q) surface 23503 raw forks the dispatch namespace. **Failure modes:** stale FK-translation table (re-raise on unknown constraint mitigates) + misparsed RAISE EXCEPTION text (per-code parse tests required). **Revisit when:** package goes public; new BCxxx code wants rich structured fields; new FK whose 23503 should map to a BcCode; real-money use case forces amount→string.
- [[reaper-schedule-deferral]] — type: decision — **Slice 6.5 reaper scheduling DEFERRED 2026-05-08.** Pre-trigger, `idempotency_keys` has zero writers in production code (per `[[idempotency-keys-schema-research]]` F7 + slice 4 migration's explicit *"do NOT look up / lock the idempotency_keys row. That live in the HTTP layer."*). Scheduling pg_cron now is hourly noop DELETE on an empty table — load-bearing ops cost (per-env dashboard `CREATE EXTENSION` step per `[[reaper-schedule-research]]` F2; composite-reaper for `cron.job_run_details` retention per F3; failed-run alerting per F1) for zero present-day benefit. **Trigger (mechanical):** any non-test, non-script TypeScript code in `apps/` or `packages/` containing `INSERT INTO idempotency_keys` / `INSERT INTO "idempotency_keys"` / `.insert(idempotencyKeys)`. Grep-detectable; the existing `scripts/check-direct-wallet-mutation.ts` lint flags it; the `// allow-direct-mutation` opt-out PR is the trigger event. Mechanical (not slice-named) because slice numbers reshuffle. **No secondary trigger** (table-size alert) — primary trigger is grep+PR-review-mediated; if both fail the discipline is broken and a monitoring alert papers over rather than fixes. Belt-and-suspenders rejected as overengineering for solo-dev with vault-discipline cascade tracking. **Rejected alternatives:** (a) ship pg_cron now — wins when production traffic is already writing OR infrastructure-first is named policy; not here. (b) staged_jobs+worker — wins when `apps/backend` is shipping the worker for other reasons; `apps/backend` is empty. Infrastructure-first counter — wins for large-team-weak-vault setups; doesn't apply. **Failure mode:** trigger fires, nobody notices, `idempotency_keys` fills to disk cap (~10 days on Free-tier 500MB / ~160 days on Pro+ 8GB at indie-projection 50MB/day/project per Brandur). Recovery: `SELECT idempotency_keys_reaper();` is ad-hoc callable. **Revisit when:** primary trigger fires.
- [[reaper-schedule-research]] — type: research — **Verification spike for slice 6.5 reaper scheduling on Supabase Free-tier (2026-05-08).** Six-question spike: pg_cron Free-tier availability, privilege model, migration shape, operability, lock-in, failure modes. Triangulation ✓ (tier 1 source-walk of `supabase/postgres@develop/nix/ext/versions.json` confirms pg_cron 1.6.4 pinned for Postgres 17; tier 2 `citusdata/pg_cron@v1.6.7` upstream + `supabase.com/docs/guides/cron` + Cron quickstart docs; tier 3 contradiction probe via Discussion #37405 + CLI Issues #647/1591/4163 + Discussion #16250). **F1 (high confidence): pg_cron is available on Supabase Free-tier without artificial gating** — staff verbatim per Discussion #37405 GaryAustin1 (Supabase Collaborator): *"Cron is only limited by the resources it uses CPU/Memory/Disk wise on any tier."* Empirical `CREATE EXTENSION pg_cron` on a real Free-tier project remains the user's open thread (requires their account). **F2 (high confidence on the contradiction): the naive "drop CREATE EXTENSION into a Drizzle migration" path is broken** — three CLI issues (#647 closed, #1591 open, #4163) document concrete `must be owner of table job (SQLSTATE 42501)` and `must be owner of function grant_pg_cron_access (SQLSTATE 42501)` failures from migration-only install. Workaround: **dashboard-enable extension once per environment, schedule via Drizzle migration**. Per-environment ops cost is real but small. **F3 (load-bearing cascade obligation surfaced): `cron.job_run_details` is not auto-cleaned** — Supabase quickstart docs verbatim: *"records in the `cron.job_run_details` table are not cleaned up automatically… will take up disk space."* BokChoy needs a *reaper of the reaper* — recommended shape is composite reaper (single function deletes both `idempotency_keys` AND `cron.job_run_details` rows) per F3 option 3. **F4 (replication clean, PITR inferred-clean):** Citus upstream verbatim: *"pg_cron does not run any jobs as long a server is in hot standby mode, but it automatically starts when the server is promoted."* No double-fire. PITR survival inferred from physical-backup + WAL-replay model — `cron.job` is a regular table, schedules survive restore (medium confidence; no direct Supabase cite found). **F5 (lock-in favorable):** pg_cron supported on AWS RDS 12.5+, Aurora 12.6+, Neon (default-preloaded at 1.6.4, matching Supabase pin). Schedule SQL is upstream-Postgres standard — no Supabase-specific dialect. **F6 (capacity comfortable):** Supabase recommends ≤8 concurrent jobs / ≤10 min/job; BokChoy's hourly reaper is 1 job × ~5ms p95 — trivially within envelope. **F7 (M1 alignment):** schedule-as-postgres lines up with the SECURITY DEFINER OWNER postgres reaper from slice 6 — bypass-RLS works regardless of caller-role. **Operational implications:** option (a) pg_cron is shippable on Free-tier today with three operational requirements: (1) per-env dashboard-enable step, (2) composite-reaper or paired retention schedule for `cron.job_run_details`, (3) failed-run monitoring on `cron.job_run_details.status='failed'`. Option (b) staged_jobs+worker remains correct *if* BokChoy commits to the outbox worker for other reasons; option (c) defer-entirely is viable pre-launch (no production traffic = no accumulation). **Open threads:** empirical Free-tier `CREATE EXTENSION` confirmation (user's account); empirical dashboard-then-migrate flow; PITR direct cite (re-search quarterly); Crunchy/Render pg_cron support (low priority); cascade to `[[local-docker]]` if option (a) lands; cascade to `[[wallet-mechanics]]` Cascade obligations adding the per-env enable step.
- [[economy-primitives-research]] — type: research — **Q5+Q6+Q7 of the wallet gap-cluster /research queue, bundled.** PlayFab Economy v2 docs (Microsoft Learn `currencies` quickstart + `transaction-history`) + pgledger (already cited) + LootLocker public docs (gated, conceptual surface only) + Stripe ledger reason-code shape (contradiction probe). **Major finding (Q5/G7):** F2P backends (PlayFab, LootLocker) do **NOT** ship a closed reason-code taxonomy publicly — operation-type enum exists at the kind-column level, business-context reason layer is open. Stripe's closed-enum is real-money-fintech-context, not F2P-applicable. Three viable shapes for BokChoy: R1 closed CHECK enum (Stripe-shape, mode-collapse), R2 open TEXT (PlayFab/LootLocker-shape, dashboard-grouping concern), R3 per-project allowlist `reason_codes(project_id, code, category, is_system)` table with FK from `transactions.reason_code` + bootstrap 12-code default-set per project (`signup_bonus`, `daily_login`, `quest_reward`, `loot_pull_reward`, `shop_purchase_grant`, `iap_grant`, `compensation`, `admin_grant`, `loot_pull_cost`, `shop_purchase_cost`, `crafting_cost`, `admin_debit`). **R3 commit** — production-aligned hybrid; reliable Month 6 faucet/drain dashboard grouping via `category` column; customer extensibility without BokChoy migration. **Q6 currencies baseline:** `(id UUID PK, project_id UUID FK, code TEXT 1-16 alphanumeric+underscore, display_name, description, decimals SMALLINT 0-8 DEFAULT 0, is_premium BOOLEAN, is_tradable BOOLEAN, created_at, updated_at)` UNIQUE(project_id, code). PlayFab's 1-3 char `FriendlyId` relaxed to 1-16 (F2P customer needs longer codes than fintech 3-char). Cap/regen/soft-cap explicitly out-of-MVP. **Q7 wallets baseline (pgledger row-per-`(player, currency)` pattern):** `(id UUID PK, project_id UUID, player_id UUID, currency_id UUID FK, balance NUMERIC(20,4) DEFAULT 0 CHECK >= 0, version BIGINT, allow_negative_balance BOOLEAN, created_at, updated_at)` UNIQUE(project_id, player_id, currency_id). JSONB-balances bag rejected (no F2P production cite, loses per-currency indexing). Lazy-create on first credit. **Cascade obligation against `[[wallet-mechanics]]` §3:** add `wallet_version BIGINT NOT NULL` column to `transactions` for forensic audit-trail (pgledger pattern); FK `transactions.reason_code → reason_codes(project_id, code)` composite-FK. Implementation order: currencies → wallets → reason_codes + bootstrap → transactions with FKs.
- [[idempotency-keys-schema-research]] — type: research — **Q4 of the wallet gap-cluster /research queue.** Triangulated Brandur `rocket-rides-atomic@94b370d` (already cited, full Postgres schema) + `stripe/stripe-node@42384847` `src/RequestSender.ts:240-360` (SDK retry behavior, source-walked) + Shopify `shopify.dev/docs/api/usage/implementing-idempotency` (parameter-mismatch via "fingerprinting", 24h TTL, distinct error codes `IDEMPOTENCY_CONCURRENT_REQUEST` vs `IDEMPOTENCY_KEY_PARAMETER_MISMATCH`). **F1 contradiction:** Brandur ships full `request_params JSONB`; Shopify ships fingerprint (hash). Both tier 1+2; choice is project-conditional. **F2 contradiction:** Brandur's `locked_at TIMESTAMPTZ DEFAULT now()` survived contradiction probe — no surveyed critique; cleaner-alternative claim (NULL-until-locked) is inference. **F4 surfaced:** Stripe's `stripe-should-retry: false|true` response header — server-controlled retry signal; vault as future-option for BokChoy. **F5 surfaced:** Stripe SDK auto-generates `stripe-node-retry-${uuid4()}` for retry-eligible POST when user doesn't supply key — BokChoy SDK should match (`bokchoy-sdk-retry-${uuid4()}`). **F6 BokChoy schema synthesis:** `idempotency_keys(id BIGSERIAL PK, project_id UUID, idempotency_key TEXT CHECK length<=255, request_method, request_path, request_params JSONB, response_status INTEGER NULL, response_body JSONB NULL, locked_at TIMESTAMPTZ NULL, completed_at TIMESTAMPTZ NULL, created_at) UNIQUE(project_id, idempotency_key)` + reaper index. JSONB chosen over fingerprint (storage cost bounded by 24h TTL); NULL-until-locked chosen over Brandur DEFAULT now() (self-documenting); no recovery_point per D2-α. Hourly reaper via staged_jobs or pg_cron. **F7 server-derived natural key path stays primary** (transactions UNIQUE on `(wallet_id, source_event_id)` IS the idempotency record); idempotency_keys table only for client-supplied-header path (designer-initiated mutations). **Open threads:** Shopify fingerprinting algorithm not disclosed; `bokchoy-should-retry` header future-option; reaper via pg_cron vs staged_jobs deferred to /implementation.
- [[tenancy-ids-research]] — type: research — **Q1 + Q2 of the wallet gap-cluster /research queue, executed in parallel.** Better Auth source-walked (`6b03a45a`) — `advanced.database.generateId: "uuid"` swaps both migration column type to native `uuid` AND runtime generator to `crypto.randomUUID()` (UUIDv4); custom-function hook available for v7 plug-in. Production survey of multi-tenant SaaS tenant-root tables (Supabase Auth UUID-native, Stripe prefixed-text, pgledger prefixed-ULID-over-UUIDv7, Vercel/WorkOS/flightcontrol commercial templates UUID-everywhere). UUIDv7-vs-UUIDv4-vs-BIGSERIAL benchmark (mblum.me Postgres 15, 44k rows): UUIDv7 BTREE inserts ~49% faster than UUIDv4, index ~25% smaller; theoretical mechanism (B-tree right-edge locality from monotonic prefix) holds at scale. BIGSERIAL enumeration-attack class (HN + OWASP + Security Boulevard + TrustedSec): leaks signup order + tenant count + creation rate — rejected for customer-facing IDs in BokChoy's threat model. **Resolutions:** **Q1 (G1) UUID wins** for `players.id` — `[[player-auth]]` §2 stands; `[[wallet-mechanics]]` §3/§5/§6 BIGINT references retire (column-type swaps + §6 HMAC→UUID projection rewrite + §6 BIGINT collision-math paragraph + 10M-player Revisit-when trigger all retire as obsolete at 122-bit width). UUIDv4-today vs UUIDv7-today is a small forking decision for /design — UUIDv4 is Postgres-native today on Postgres 17 (`gen_random_uuid()` + `crypto.randomUUID()`), UUIDv7 requires transcribing pgledger's MIT-licensed ~10-line SQL helper or waiting for Supabase Postgres 18; both reversible. **Q2 (G2) `projects` baseline schema:** `id UUID PK DEFAULT gen_random_uuid(), organization_id UUID FK to Better Auth "organization", name TEXT, slug TEXT (UNIQUE per org), is_child_directed BOOLEAN per [[player-auth]], status TEXT CHECK ('active'/'paused'/'archived'), settings JSONB, created_at, updated_at`. Deferred: env model (separate-project-per-env per `[[idempotency-strategy]]`), API key surface (separate `api_keys` decision), soft-delete posture, region, billing (lives on org). **Cascade obligations:** Better Auth config must set `advanced.database.generateId: "uuid"` (else `organization.id` is TEXT 32-char alphanumeric and the entire RLS GUC chain breaks at first cast); Drizzle table definitions hand-translate Better Auth schema; `api_keys` schema is a follow-on /design decision. **Rejected alternatives** with named winning conditions: BIGSERIAL (rejected on enumeration-attack class), prefixed-text (rejected on Postgres-stack RLS GUC casting + tooling friction; revisit if support-engineering UX becomes a documented bottleneck). **Open threads:** Q4 idempotency_keys schema (project_id UUID confirmed, full column set still pending), Q5 reason codes, Q6 currencies, Q7 wallets, Q8 UUIDv7 helper transcription cost, Q9 Better Auth migration co-existence with drizzle-kit-only commitment, Q10 Supabase managed-tier Postgres 18 timeline.
- [[wallet-functions-research]] — type: research — **Q3 of the wallet gap-cluster /research queue: pgledger source-walked + Postgres 16 docs + contradiction probe.** **Major finding:** `[[wallet-audit-invariant-research]]` S3 misattributed M2 to pgledger — pgledger ships **M1** (app-library + ergonomic discipline), zero `SECURITY DEFINER`, zero `GRANT/REVOKE/CREATE ROLE`, direct `UPDATE pgledger_accounts` shown as normal usage in `examples/lock-account.sql`. **BokChoy's M2 in `[[wallet-mechanics]]` §2 has no surveyed production cite** — it's a docs-cited synthesis (Postgres 16 SECURITY DEFINER + REVOKE/GRANT pattern + table-level REVOKE on app role). Two paths for /design: (A) keep M2 with downgraded cite tier, (B) downgrade to M1 (pgledger-actual). **Useful structural template extracted from pgledger:** sorted-then-locked FOR UPDATE deadlock prevention (contradicts `[[wallet-mechanics]]` SERIALIZABLE commitment — forking decision), update-then-check ordering, balance-update-before-audit-row sequencing, per-account `version BIGINT` forensic column, prefixed-ULID ID format (cross-feeds G1 as a real third option). **Named gaps where pgledger gives no template** (BokChoy must derive elsewhere): idempotency-key handling, tenant-context check via `current_setting('app.current_tenant')::UUID`, custom SQLSTATE error codes (`P0xxx` range pinned by /design), search_path hardening. **Single-entry vs double-entry deliberate divergence flagged** — pgledger ships pure double-entry with system "liquidity" accounts; BokChoy single-entry because virtual currency is created/destroyed by the system. Erratum owed to `[[wallet-audit-invariant-research]]` S3 + F2 + F3. Amendments owed to `[[wallet-mechanics]]` §2 (cite tier) + concurrency line 466 (forking pick) + observability line 468 (OTel-from-plpgsql is unfeasible-as-stated). Four open threads queued (pgledger perf, Brandur Ruby translation, OTel-from-plpgsql, closed-source production systems Stripe/RevenueCat/Shopify).
- [[gaps]] — type: discovery — wallet feature gap cluster (7 gaps) blocking first implementation slice; written 2026-05-04 in /implementation, queued for /research → /design resolution.

## inventory

- [[inventory/inventory-contract]] — type: decision — **Inventory primitive contract RESOLVED 2026-05-19.** Eight sub-decisions grounded in `[[inventory/.research/inventory-primitive-research]]`: **(i) Item model = M-A** (per-definition `stackable: bool` flag + count column; single inventory table with conditional partial UNIQUE indexes for stackable-vs-instance split — no two-table separation). Defended over M-B (PlayFab named-multi-stack + per-stack JSONB — pays for rolled-stats use case not yet on customer roadmap), M-C (LootLocker pure per-instance — row growth + per-row overhead for consumables), M-D (AccelByte slot-based — operational complexity with no v1 customer requirement). **(ii) Schema:** new `items(id, project_id, code, display_name, description, stackable, max_count nullable, created_at, updated_at)` catalog + new `inventory(id, project_id, player_id, item_id, instance_id nullable, count, properties JSONB, version, created_at, updated_at)` ownership; partial UNIQUE indexes `(project_id, player_id, item_id) WHERE instance_id IS NULL` for stackable + `(project_id, player_id, instance_id) WHERE instance_id IS NOT NULL` for instances; RLS `tenant_isolation` mirrors every other tenant-scoped table. Two new SQL functions `item_grant_by_external_id` + `item_consume_by_external_id` analogous to `wallet_credit_by_external_id` per `[[wallet/credit-route-contract]]`. **(iii) Routes** (all player-centric REST symmetric to wallet routes): `POST /v1/players/{playerExternalId}/inventory/{itemCode}/grant` + `.../consume` + `GET /v1/players/{playerExternalId}/inventory` (paginated list) + `GET /v1/players/{playerExternalId}/inventory/{itemCode}` (single-item). Auth chain inherits M-1.5: `apiKeyMiddleware` + `sValidator('param')` + body/query validators + `idempotencyMiddleware` on mutating routes (not GETs). Response 201 bare-data with discriminated-union shape (`{stackable: true, newCount, version}` vs `{stackable: false, instanceIds}`). **(iv) Item identification = `code` (slug)**, regex `^[A-Za-z0-9_]{1,64}$` — derived from production data (longest realistic name `battle_pass_season_07_legendary_reward_chest_variant_3` 54 chars ≈ 85% of budget); charset matches currency `code` for class consistency; mixed-case allowed (operator-customer-facing display); rejected 32-char (eats verbose case), 128-char (matches `external_id` but invites junk-drawer usage), 255-char (header-class budget wrong scale). **(v) Soulbound/tradability NOT shipped at MVP** per F2P SDK class 0:4 omission (`[[inventory/.research/inventory-primitive-research]]` F4); schema-additive when first peer-trading customer signals. **(vi) Stack capacity = C-Reject** on overflow: `BC081 InventoryOverflow` → HTTP 422 `INVENTORY_OVERFLOW` with `{currentCount, requestedAmount, maxCount, availableCapacity}` body. Symmetric to wallet `BC010 InsufficientFunds`. Rejected C-Clamp (silent intent-corruption inconsistent with wallet path), C-Mailbox (depends on Month 6 primitive). **(vii) Operation set at MVP = grant + consume only** (no `set`, no `delete-entire-stack`, no `transfer`). PlayFab's `UpdateInventoryItems` deferred until customer signal; transfer-between-players deferred to peer-trading slice if it ever lands. **(viii) Audit log reuse:** `transactions.kind` CHECK already enumerates `item_grant`/`item_consume`/`compensation_grant` per `packages/db/src/schema/wallet.ts:185-186`; `transactions.item_id`/`item_quantity` columns already present per `wallet.ts:168-169`; `transactions.wallet_id` nullable → item-only rows leave wallet_id NULL. **No transactions schema change owed for the inventory primitive.** **6 rejected alternatives with winning conditions:** M-B, M-C, M-D, C-Clamp, C-Mailbox, `set`-at-MVP, soulbound-at-MVP. **3 failure modes:** schema-evolution lock-in at first non-F2P partner (mitigation: M-A → M-B/M-C is non-breaking-additive, revisit-when 1 week post-signing); inventory row growth unbounded in pathological non-stackable case (mitigation: row-count observability + partition migration trigger 1M); item-code-vs-reason-code collision (mitigation: Cockpit UI labels, SDK JSDoc, no backend invariant violation). **6 revisit-when triggers** (per-instance metadata partner, `set` semantics, peer-trading marketplace, Mailbox primitive landing, 1M rows per project, INT32 ceiling). **13 cascade obligations for /implementation:** migration 0011 (items + inventory tables) + migration 0012 (SQL functions) + Drizzle schema + 4 wrapper files + backend routes + Zod schemas + error envelope (BC080/BC081/BC082) + SDK methods + SDK error classes + OTel spans + smoke-script extension + mvp-feature-sequence amendment + CONTEXT.md `**Item:**` term. **Two /implementation picks flagged inline:** (a) module location for wrappers — new `packages/inventory/` vs extend `packages/wallet/`; (b) SDK namespace — `bokchoy.inventory.*` vs sibling to `wallets.*`. Confidence: high.
- [[inventory/.research/inventory-primitive-research]] — type: research — **Inventory primitive shape across the F2P game-economy SDK class LANDED 2026-05-18.** 6 in-class sources (PlayFab Economy v2 Inventory APIs × 2 pages, Nakama Hiro inventory concept, LootLocker Server API List Player Inventory, LootLocker docs concept, AccelByte Inventory Service concept) + 1 contradiction probe (Steam Inventory Service schema — cross-platform peer-trading class). **F1 LOAD-BEARING (3:1 SPLIT):** items and currencies are split in 3 of 4 F2P SDKs surveyed (Nakama explicit "Inventory and currencies remain distinct"; LootLocker separate `/server/player/{id}/inventory` and `/wallet` endpoints; AccelByte Inventory ≠ Commerce service). PlayFab unifies (Virtual Currency is `Type` in catalog alongside items) — genuine outlier. BokChoy's already-shipped wallet schema (currencies + wallets tables) is class-aligned with the 3-of-4 majority; staying split is the dominant pick. **F2 (no class convergence on item model):** 4-way fork — (M-A) per-definition stackable flag + count column (Nakama Hiro), (M-B) named multi-stack with per-stack JSONB metadata (PlayFab `StackId` + `DisplayProperties`), (M-C) pure per-instance UUID (LootLocker), (M-D) slot-based (AccelByte). Each defensible for different game shapes — inventory-bag, per-stack-customized, MMO-unique, slot-based-deckbuilder respectively. **F3 (slug/code dominates F2P, 3:1):** PlayFab AlternateIds friendly-name `"LaserSword"`, Nakama Hiro `"iron_sword"`, AccelByte `code` — string slug is the customer-facing identifier. LootLocker uses numeric `instance_id` + `variation_id`. Steam (probe) integer `itemdefid` only. BokChoy's currency `code` regex `^[A-Za-z0-9_]{1,16}$` is class-compatible for items. **F4 (soulbound 0:4 in F2P SDK class):** uniformly omitted as first-class data-model field across Nakama, LootLocker, AccelByte; PlayFab V2 explicitly DROPPED V1's tradable flag. Steam (cross-class) ships `tradable: bool`, `marketable: bool`, `game_only: bool` + temp-untradable-after-purchase anti-fraud — surfaces conditions under which F2P consensus breaks (peer-trading marketplace requirement). **F5 (URL shape — player-centric REST or RPC body-encoded, zero item-centric):** LootLocker `GET /server/player/{player_id}/inventory`; PlayFab RPC-style POST with `Entity.Id` in body; Nakama RPC. **No item-centric URL cite.** BokChoy's existing player-centric wallet shape per `[[wallet/credit-route-contract]]` is class-idiomatic. **F6 (idempotency — 1:4, PlayFab-only):** PlayFab explicit `IdempotencyId` 14-day window across all inventory APIs; Nakama Hiro / LootLocker / AccelByte silent. Insufficient class signal to deviate from BokChoy's Brandur-shape 24h reaper per `[[architecture/idempotency-strategy]]`. **F7 (read shape — paginated dominant 3:4):** PlayFab Collections + ContinuationToken; LootLocker `{total, items[]}` flat; AccelByte paginated. BokChoy's Stripe-style cursor per `[[wallet/balance-history-contract]]` G11.4 is class-compatible. **Conflicts:** PlayFab unified vs Nakama/LootLocker/AccelByte split — class-membership conflict, resolved by 3:1 majority + BokChoy's already-shipped split schema; Steam tradability vs F2P omission — not a conflict, class-mismatch finding. **Operational implications (10 items for /design):** Q1 SPLIT, Q2 lean M-A (Nakama-shape) for MVP simplicity, Q3 per-definition `stackable: bool`, Q4 `code` slug per regex, Q5 NO soulbound at MVP, Q6 player-centric REST symmetric to wallet `POST /v1/players/{externalId}/inventory/{itemCode}/grant`, Q7 Brandur-shape unchanged, Q8 Stripe-cursor pagination, Q9 purchase composes Shop primitive, Q10 transactions.kind already supports item_grant/item_consume — substrate reusable, no audit-log migration. **5 open threads** carried (AccelByte API depth, Nakama RPC method names, PlayFab purchase_limit, history endpoint scope, catalog editor UX). Confidence: high.

## cockpit

- [[cockpit/turbopack-flag-research]] — type: research — **`--turbopack` flag in `package.json` dev script: documented no-op-when-default-applies LANDED 2026-05-14.** Triggered by user halt on the F8 "tooling cleanup" item from `[[cockpit/nextjs-16-proxy-research]]`. F8 had made an unsourced "harmless / safe to drop" claim — training-data inference. Verified: per Next.js 16.2.6 CLI docs (`lastUpdated: 2026-05-13`) the `--turbopack` row in `next dev` options reads verbatim *"Force enable Turbopack (enabled by default). Also available as `--turbo`."* — the flag is documented, supported, NOT deprecated, NOT warned, NOT errored. Idempotent (force-enable when already-enabled = no-op). Empirical confirmation: cockpit's own slice 8.3.6 dev gate ran `next dev --turbopack -p 3001` and produced `▲ Next.js 16.2.6 (Turbopack) ✓ Ready in 1076ms` with no warning. Community Q&A `vercel/next.js#83308` confirms removal is safe AND retention is safe. **F2:** both paths valid — keep flag (explicit opt-in style; cheap insurance vs future default flip) OR remove (v16-canonical style; aligns with cockpit's already-flagless `build` script). No engineering reason to prefer one. **F8 amendment to `[[cockpit/nextjs-16-proxy-research]]`:** "flag is recognized for backward compat" → "flag is the documented force-enable form; removal is the v16-canonical style but retention has no runtime cost." **`state.md` Next-on-resume amendment owed:** the "package.json --turbopack cleanup" item is NOT a slice — at most a one-line edit, optional. Demote to backlog note or drop. Sources: 1× Tier-2 (Next.js CLI docs), 1× Tier-1 (cockpit's own dev gate output), 1× Tier-6 (vercel/next.js Discussion #83308). Confidence: high. **2 open threads** (Next.js 17 timeline; Vercel deployment-build behavior — currently moot since cockpit's `build` script is already flagless).
- [[cockpit/nextjs-16-proxy-research]] — type: research — **Next.js 16 `middleware.ts` → `proxy.ts` rename research LANDED 2026-05-14.** Triggered by /implementation halt on slice 8.3.6 — the implementation seat was about to write `middleware.ts` from training-data defaults; per `[[feedback-fast-moving-oss-research-first]]` Next.js framework majors are exactly the class of moving target where research-first is mandatory. **F1 LOAD-BEARING:** `middleware.ts` is deprecated in `v16.0.0`, renamed to `proxy.ts`; function `middleware()` → `proxy()`; config flag `skipMiddlewareUrlNormalize` → `skipProxyUrlNormalize`. Backward compat: `middleware.ts` still works in 16.x with deprecation warning, removed in future major. Codemod: `npx @next/codemod@canary middleware-to-proxy .`. **F2 LOAD-BEARING:** `proxy.ts` runs on Node.js runtime — Edge Runtime NOT supported, the `runtime` config field throws if set. Next.js team named an escape hatch (*"keep using `middleware`"* for Edge) with a *"follow up on a minor release"* pending. For cockpit auth gate: `getSessionCookie` is pure cookie parsing, runtime-agnostic. The *"edge-level"* terminology in `state.md` + `[[cockpit-stack-integration-research]]` is now stale framing. **F3:** `getSessionCookie` signature from `better-auth/cookies` (Better Auth `1.6.9`): `(request: Request | Headers, config?: {cookiePrefix?, cookieName?, path?}) => string | null`. Default cookie name `better-auth.session_token` (auto `__Secure-` in prod). Cockpit `packages/auth-config/src/index.ts` has no custom `cookiePrefix` → no config arg needed. **F4:** matcher syntax unchanged (path-to-regexp anchored from start; `:path*` zero-or-more); slice 8.3.6 uses `matcher: ['/projects', '/projects/:path*']`. **F5 (PARK):** Server Functions are POSTs to the route they're used in; proxy matcher that excludes a path also excludes its Server Functions. Docs explicit: *"Always verify authentication and authorization inside each Server Function rather than relying on Proxy alone."* No current Server Actions in cockpit. **F6:** audit of 14 other Next.js 16 breaking changes against current cockpit state — 12 are ✓-none (cockpit on v16 shape via scaffold slice 8.4.0); 2 follow-on audits owed (`--turbopack` flag possibly redundant in `package.json` scripts; codemod-sweep verification pass). **F7:** canonical `proxy.ts` shape for slice 8.3.6 vaulted (3 imports + 1 named-export `proxy` function + 1 matcher config — ~15 LOC). Defense-in-depth: layout's `getSession()` stays as authoritative validation. **F8:** follow-on tooling audits enumerated. **C1:** "edge-level" terminology stale per Next.js 16 docs; amendment owed (rephrase to "proxy-tier optimistic cookie check, Node.js runtime"). Per *Contradiction protocol* current docs win. **C2:** Better Auth Issue #6360 doc-bug (over-broad "THIS IS NOT SECURE" disclaimer) — affects doc reading, not the cockpit's defense-in-depth stack. **3 cascade amendments owed:** `state.md` slice-8.3.6 framing; `[[cockpit-stack-integration-research]]` F6 row 5 + new row for the rename + Source 4 mental-model amendment; `[[cockpit/auth-surface-mount]]` (V3) rewrites-after-proxy execution-order verification. Sources: 2× Tier-2 (Next.js 16.2.6 docs `lastUpdated: 2026-05-13` — file-convention + upgrade guide), 1× Tier-2 (Better Auth `integrations/next`, v1.6.x), 1× Tier-1 (`Achour/nextjs-better-auth/proxy.ts` production cite), 2× Tier-6 (Better Auth Issues #6853 + #6360 for context, both closed unresolved). Confidence: high. **4 open threads named.**
- [[cockpit/shadcn-setup]] — type: decision — **shadcn bootstrap shape RESOLVED 2026-05-12 from `[[cockpit/shadcn-setup-research]]`.** Four picks: (1) **`--base radix`** — Radix UI primitives, NOT Base UI. User defense: *"ecosystem maturity matters more than upstream signal"* — inverts the lock-in-tax-on-deprecation attack (Radix exists independently of shadcn-as-CLI; thousands of OSS consumers; production-cited since 2022). (B-BASE) primitive-coverage gap at 2026-Q2 unresearched + user declined to gate slice 8.4 on additional /research. (2) **`--preset radix-nova`** — Lucide / Geist / neutral / radius=default / chart=neutral / menuAccent=subtle / menuColor=default. User defense: *"it's an admin tool plus Lucide has more icons and more mature"* — admin-tool register defangs visual-undifferentiation attack (blandness = operator-familiarity for B2B cockpit inspecting transactions/API keys); Lucide ~1500 catalog defeats Phosphor-coverage-gap concern at slice 8.5-8.6 glyph needs; cross-cutting benefit Geist matches slice 8.3.1 verbatim (zero font swap). (3) **Dark mode = `next-themes` class-based** — FORCED MOVE not a fork; shadcn `@custom-variant dark (&:is(.dark *))` requires `.dark` class selector, doesn't activate from media query. (4) **(M-LOCAL) cockpit-local primitives** — `apps/cockpit/components/ui/` (`aliases.ui: "@/components/ui"`); previously settled per user 2026-05-12 directive + `[[cockpit/shadcn-setup-research]]` F8 (Issue #6878 OPEN — monorepo `packages/ui/` extraction has Tailwind v4 content-scanning bug). **Init invocation:** `cd apps/cockpit && bunx shadcn@latest init --base radix --preset nova --yes` (must run from app workspace; CLI exits at monorepo root). **components.json shape AS-GENERATED (post-init verified 2026-05-12)** — `style: "radix-nova"` (combined base+preset identifier; NOT "new-york" — that name comes from shadcn's older apps/v4 site; current init writes the combined form), `rsc: true`, `tsx: true`, `tailwind.config: ""` (Tailwind v4 has NO JS config), `tailwind.css: "app/globals.css"`, `baseColor: "neutral"`, `cssVariables: true`, full aliases map, `iconLibrary: "lucide"`, plus init-added `rtl: false`, `menuColor: "default"`, `menuAccent: "subtle"`, `registries: {}`. **6 rejected alternatives with named winning conditions:** (B-BASE), (B-OPT-OUT), (P-LYRA), (P-SERA), (P-CUSTOM), (DM-MEDIA-QUERY), (M-SHARED). **4 failure modes with mitigations:** init-time globals.css overwrite (mitigation: pre-init `git add -A apps/cockpit/`), preset → token cascade one-way at init time (mitigation: `--force` re-run from clean tree), Lucide icon-not-found mid-slice (mitigation: custom SVG to `components/ui/icons/`, do NOT add second icon lib), monorepo-root init exit (mitigation: documented `cd apps/cockpit` discipline). **6 revisit-when triggers** named. **2 cascade amendments LANDED in same /design seat:** `[[cockpit/file-structure]]` cascade #3 (`tailwind.config.ts`) marked OBSOLETE; `[[cockpit/nextjs-scaffold-research]]` F4 SUPERSEDED for shadcn-adopting cockpit (create-next-app canonical vs shadcn-v4 canonical reconciled via *Contradiction protocol* — shadcn shape wins for consumers adopting shadcn). **7-step implementation runbook** vaulted for slice 8.4 (deps + scaffold + theme-provider + git stage + init + verification + cascade amendments confirmation). Confidence: high.
- [[cockpit/shadcn-setup-research]] — type: research — **Current shadcn CLI canonical setup for Next.js 16 + Tailwind v4 + RSC LANDED 2026-05-12.** Triangulated against `shadcn-ui/ui` repo HEAD (pushed 2026-05-12T08:27:02Z) + `ui.shadcn.com/docs/{installation/next,tailwind-v4}` + GitHub issue contradiction probe. **F1 LOAD-BEARING:** package renamed `shadcn-ui` → **`shadcn`** at npm; current version **`4.7.0`**. 2.x-era `npx shadcn-ui@latest init` is WRONG; correct is `bunx shadcn@latest init`. **F2 LOAD-BEARING:** init prompt flow restructured — old style/baseColor/iconLibrary individual prompts REMOVED; new flow asks `base` (radix vs **base-ui**) then **named preset** that bundles full look-and-feel. **F3:** 7 named presets — Nova (Lucide/Geist/neutral — default `--defaults`=`base-nova`), Vega, Maia, Lyra, Mira, Luma, Sera. **F4 LOAD-BEARING:** components.json schema — `style` (string), `rsc` (true for App Router), `tailwind.config: ""` (EMPTY — Tailwind v4 has no JS config), `tailwind.css`/`tailwind.baseColor`/`tailwind.cssVariables: true`, `aliases.{components,utils,ui,lib,hooks}`, optional `iconLibrary`/`rtl`/`menuColor`/`menuAccent`/`registries`. **F5 LOAD-BEARING:** dark-mode mechanism is `next-themes` class-based — `<NextThemesProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>` + `suppressHydrationWarning` on `<html>`. Canonical theme-provider bundles a `d`-key dark/light hotkey with typing-context guards. **F6 LOAD-BEARING:** canonical `globals.css` shape — `@import "tailwindcss"; @import "tw-animate-css"; @import "shadcn/tailwind.css";` + `@custom-variant dark (&:is(.dark *));` + `@theme inline` with 30+ tokens (background/foreground/card/popover/primary/secondary/muted/accent/destructive/border/input/ring + 5 chart variants + 8 sidebar variants) + `:root`/`.dark` OKLCH (not hex) token sets + `@layer base { * { @apply border-border outline-ring/50; } }`. Slice 8.3.1's globals.css is structurally OUT OF DATE. **F7 LOAD-BEARING:** `shadcn/tailwind.css` is exported infrastructure CSS (1669 bytes) — `@custom-variant data-{open,closed,checked,unchecked,selected,disabled,active,horizontal,vertical}` wires Radix/Base UI data-state attributes to Tailwind v4 selectors. NOT optional. **F8 LOAD-BEARING:** monorepo `packages/ui/` extraction has KNOWN OPEN BUG (Issue #6878, Tailwind v4 content scanner doesn't find class names from primitives in different workspace package — destructive button has invisible text). User directive 2026-05-12 ("only cockpit will use it") sidesteps this entirely via (M-LOCAL) `apps/cockpit/components/ui/`. **F9:** `shadcn init` must run from `apps/cockpit/` (NOT monorepo root — CLI exits at monorepo root). **F10:** NEW `(B-RADIX) vs (B-BASE)` design fork — `--base radix` (mature, Radix UI) vs `--base base` (Base UI by Floating UI team, shadcn-recommended). Out of scope for this entry; needs Base UI maturity research before pick. **F11:** dependency cascade — add `next-themes ^0.4.6` + `tw-animate-css` to cockpit deps before init; `prettier-plugin-tailwindcss` optional devDep. **F12:** `next.config.ts` is NOT touched by init — cockpit's cacheComponents/reactCompiler/turbopack.root/rewrites survive intact. **3 conflicts surfaced:** (C1) `[[cockpit/nextjs-scaffold-research]]` F4's globals.css canonical is `create-next-app`'s (pre-shadcn) shape; shadcn-v4's wins for cockpit (sequencing). (C2) `[[cockpit/file-structure]]` cascade #3 (`apps/cockpit/tailwind.config.ts`) is OBSOLETE — Tailwind v4 eliminates JS config. (C3) slice 8.3.1's `@theme` block has 4 tokens; shadcn primitives need 30+ — `bg-card`/`text-muted-foreground`/`border-border`/`ring-ring` won't resolve at copy-in. Operational: 13 cascade obligations for /design (pick B-base + preset + dark-mode switch confirm) + /implementation (deps + globals.css rewrite + theme-provider + hooks/ dir + init invocation `cd apps/cockpit && bunx shadcn@latest init --base <pick> --preset <pick> --yes`). Confidence: high.
- [[cockpit/nextjs-scaffold-research]] — type: research — **Cockpit `apps/cockpit/` scaffold audit vs canonical Next.js 16 + Tailwind v4 + TS setup LANDED 2026-05-12.** Triangulated against `vercel/next.js@9a48c22c` `create-next-app` template + generator (Source 1+2) and `nextjs.org/docs` v16.2.6 / lastUpdated 2026-05-07 across `cacheComponents`, `reactCompiler`, `turbopack`, `typescript` config pages (Sources 3–6). **F1 LOAD-BEARING:** cockpit ships NO linter — no `eslint`/`eslint-config-next`/`biome` dep, no `lint` script, no `eslint.config.mjs` or `biome.json`; canonical create-next-app ships ESLint default + Biome optional. BLOCKER for F6/F8 anti-pattern enforcement from `[[cockpit-stack-integration-research]]`. **F2 LOAD-BEARING:** `tsconfig.json` deviations — `jsx: "preserve"` (canonical: `"react-jsx"`), `types: ["node"]` (canonical: unset; restricts global @types/react-dom), `include` missing `.next/dev/types/**/*.ts` + `**/*.mts` (breaks `typedRoutes` dev-mode TS checks). **F3 LOAD-BEARING:** monorepo root `.gitignore` (verified 2026-05-12) MISSING `next-env.d.ts` entry — canonical gitignore ships it; docs explicitly recommend gitignoring; must add before any cockpit commit. **F4 LOAD-BEARING:** `app/globals.css` is bare (`@import "tailwindcss"` only); canonical adds `@theme inline` block + CSS variables + dark-mode media query + body defaults. shadcn primitives (slice 8.4 dep) require the `@theme` tokens to resolve `bg-background`/`text-foreground`/`border` utilities. Will break shadcn at copy-in. **F5:** no `next/font/google` Geist setup (canonical default). **F6:** `turbopack.root` manual pin likely redundant in Next.js 16 — docs say lockfile auto-detect via `bun.lock` (present at root). **F7:** missing `public/favicon.ico` + `.env.example`. **F8:** cockpit correctly placed at top-level — `cacheComponents: true`, `reactCompiler: true`, `babel-plugin-react-compiler@^1.0.0`, Tailwind v4 PostCSS plugin shape, `lib/query-client.ts` `React.cache` wrap. 11-item ordered punch list for slice 8.3.1 scaffold-polish; ESLint vs Biome pick + `jsx: "preserve"`-vs-`"react-jsx"` perf delta carried as open threads. Confidence: high. **Amendments 2026-05-12 (slice 8.3.x item #10 verification):** (a) **F6 FALSIFIED** — pin is load-bearing, not redundant; `next dev` with pin removed selected stray `/home/mvula/package-lock.json` (operator `$HOME`) as workspace root over monorepo `bun.lock`. F6 walkback inline. Pin restored. (b) **NEW F9 LOAD-BEARING** — `next.config.ts` ESM-mode loader emits CJS to `.compiled.js`, crashes on load when source has any runtime `import` (e.g. `node:path`, `node:url`) under `"type": "module"`. Use language built-ins only: `new URL(rel, import.meta.url).pathname` for paths. Slices 8.3.0/8.3.1 verified typecheck + lint only — never ran `next dev` — so the bug shipped invisibly. Operational #10 RESOLVED + new operational #12 added (block PRs editing `next.config.ts` without `bun run dev` verification).
- [[admin-list-endpoints-contract]] — type: contract — **Slice 8.3 + 8.4 admin endpoint contracts RESOLVED 2026-05-11 from `[[admin-list-endpoints-research]]`.** Six endpoint contracts: `GET /v1/orgs/me` (8.3) + `GET /v1/projects` (8.3) + `POST /v1/projects` (8.4) + `POST /v1/projects/{id}/api-keys` (8.4) + `GET /v1/projects/{id}` (8.4) + `DELETE /v1/projects/{id}/api-keys/{keyId}` (8.4). Six convention picks ratified by user: **(R2)** bare-data success + Stripe-wrapped errors per `[[wallet-http-contract]]` G5 — production-cited × 3. **(Pa-none)** no pagination at MVP; bare arrays. Cursor pagination via Stripe `starting_after`/`ending_before` adopted post-MVP. **(A1)** nested `apiKeys[]` on `GET /v1/projects/{id}` detail — production-cited × 2 (Vercel + Stripe). Powers T2 verify-loop polling single-fetch. **(E2)** split-POST: `POST /v1/projects` creates project; `POST /v1/projects/{id}/api-keys` creates first key separately. Cockpit makes 2 sequential requests with `Idempotency-Key: cockpit-first-key-${projectId}` on the second for retry-safe failure recovery via `[[idempotency-strategy]]` D2-α + slice 8.1b idempotencyMiddleware. (E1) bundling rejected on scope-mismatch with `[[wallet-http-contract]]` G5 customer-profile-axis defense (G5 covered wire-shape per-call; bundling-vs-splitting is request-flow once-per-customer). **(Disc-no)** no `object` discriminator — 2-of-3 production majority + internal consistency. **(Pref-raw)** raw UUIDs — internal consistency with slice 8.1+. **All endpoints behind `adminGate`** per `[[admin-auth-surface]]` with statement extensions: `project: ['create', 'read']` + `apiKey: ['create', 'revoke']` + `org: ['read']`. **6 rejected alternatives** with named winning conditions. **3 failure modes with mitigations:** (E2) race-window (Idempotency-Key mitigation); idempotency-key collision on same-slug retries (use projectId not slug in key); apiKeys[] overflow at scale (cap response + truncated flag). **6 revisit-when triggers** named. **7 cascade obligations** for slice 8.3 + 8.4 implementation: auth-config statement extension; `apps/backend/src/projects/` new module with 6 handlers; Drizzle schema verification (`projects.name`, `(organization_id, slug)` unique, `api_keys.name`, `api_keys.revoked_at`); `mountProjectsRoutes(app)` in `apps/backend/src/index.ts`; OTel spans per handler; smoke-8-2.0 extension for 6 new endpoints + (E2) retry flow; cockpit TanStack Query hooks + RSC patterns per `[[cockpit-stack-integration-research]]`.
- [[admin-list-endpoints-research]] — type: research — **Slice 8.3 + 8.4 backend handler contract patterns LANDED 2026-05-11.** 4-sub-question query (a) response envelope, (b) pagination, (c) nested sub-resources, (d) create-with-side-effect bundling — triangulated across 3 production B2B SaaS APIs (Stripe / Vercel / Resend docs cite + scalar+list source-walks). **F1 LOAD-BEARING:** bare-data scalar envelopes production-cited × 3 (Stripe with `object: 'TYPE'` discriminator; Vercel + Resend bare without). `[[wallet-http-contract]]` G5 (R2) bare-success-Stripe-wrapped-errors directly confirmed for slice 8.3/8.4. (R1) `{ data: ... }` wrap REJECTED. **F2:** list envelope diverges — Stripe `{ object: 'list', data, has_more, url }` is production-cited at scale; Vercel + Resend bare arrays at MVP scale. For BokChoy at MVP no-pagination per (Pa-none) — bare arrays. When pagination introduced (slice 8.6+ transaction inspector OR >50 projects per org), adopt Stripe envelope. **F3:** cursor pagination via `starting_after`/`ending_before`, default `limit: 10`, max `100`, no `total_count` — Stripe canonical. **F4:** nested sub-resource arrays on scalar detail endpoints production-cited × 2 (Vercel heavy nesting 15+ fields; Stripe nested + expandable IDs). Confirms (A1) `apiKeys[]` nested on `GET /v1/projects/{id}` response for verify-loop polling. (A2) separate `/api-keys` endpoint rejected — doubles round-trips for T2 verify loop. **F5 LOAD-BEARING contradiction:** create-with-side-effect bundling NOT production-cited — Stripe / Vercel / Resend all split parent + child creation into separate POSTs. **Walks back prior /design (E1) bundling pick.** Two paths for /design to weigh: (E2) production-canonical split-POST (`POST /v1/projects` then `POST /v1/projects/{id}/api-keys`) — adds race window + recovery surface; (E1) BokChoy-specific bundling — defensible on customer-profile axis from `[[wallet-http-contract]]` G5 + `[[wrapper-shape]]` Fork 2 (developer-facing-SDK-during-dev winning condition). /design owes the pick. **F6:** `object` type-discriminator field non-convergent — Stripe yes (1-of-3); Vercel + Resend no (2-of-3). Internal-consistency with slice 8.1c handlers (no `object` field) + 2-of-3 majority → recommend NO `object` field. **Operational implications:** concrete contract shapes for `GET /v1/orgs/me` + `GET /v1/projects` + `POST /v1/projects` + `GET /v1/projects/{id}` enumerated. Bare-data + Stripe-wrapped-errors + no-pagination-at-MVP + nested-`apiKeys[]` + no-`object`-field + (E1)/(E2) decision owed. **6 open threads:** GraphQL comparison, GitHub Link-header pagination, PlayFab/Unity GS API peer comparison, Better Auth's own admin convention (already vaulted in `[[better-auth-org-admin-research]]`), `object` discriminator revisit-when, resource ID prefix convention (Stripe-style `proj_` vs raw UUIDs). Confidence: high.
- [[auth-surface-mount]] — type: contract — **Cockpit auth surface mount config RESOLVED 2026-05-11.** Three concrete contracts: (V3) cockpit `next.config.ts` async `rewrites()` driven by `BOKCHOY_BACKEND_URL` env var — proxies `/api/auth/:path*` + `/v1/:path*` to backend per `[[cockpit-stack-integration-research]]` F1 (P1) reverse-proxy. (H) backend `apps/backend/src/index.ts` mounts Better Auth via `app.on(['POST', 'GET'], '/api/auth/*', (c) => auth.handler(c.req.raw))` — canonical 3-LOC per Source 1 verbatim; NO `cors()` middleware under (P1) since rewrite preserves same-origin perception. (T-multi) `packages/auth-config/src/index.ts` `trustedOrigins` array with prod + www + localhost + Vercel preview wildcard (`'https://*-bokchoy.vercel.app'`) — wildcard pattern per Better Auth Source 4 demo. (P) OAuth provider config shape: `socialProviders: { google: {clientId, clientSecret}, github: {...} }` reading from `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`/`GITHUB_CLIENT_ID`/`GITHUB_CLIENT_SECRET` env. (CK) cookie config: Better Auth defaults `SameSite=Lax` + `Secure=true` in prod + `HttpOnly` — no `crossSubDomainCookies` config under (P1). (CL) cockpit `lib/auth-client.ts` uses `createAuthClient({ baseURL: '/' })` + `organizationClient()` plugin — relative path via Vercel rewrite. **5 rejected alternatives:** (V1) static JSON (no env-var interpolation), (V2) `vercel.json` env substitution (not supported by schema), `cors()` middleware (defensive-but-unused under P1), absolute backend URL in cockpit client (forfeits first-party cookie), (T-prod) prod-only trustedOrigins (breaks dev). **3 failure modes:** Vercel rewrite host-header bug (mitigation: smoke test at slice 8.3 acceptance); OAuth preview-deploy URL mismatch (Google exact-match policy; mitigation (a) skip-OAuth-on-preview, email/password fallback only); env var typo at startup (mitigation: throw on missing in non-dev). **6 revisit-when triggers** named. **1 inline /design decision:** hardcode `trustedOrigins` as module-level constant in `auth-config/src/index.ts` beside `roles` export — matches D3 static-AC pattern from `[[admin-auth-surface]]`. **Open thread:** OAuth provider preview-deploy strategy (skip-on-preview recommended).
- [[first-run-journey]] — type: decision — **Cockpit first-run journey RESOLVED 2026-05-11** per user ratifications across two derivation rounds. Concrete 11-step sequence marketing → OAuth → org-auto-create → project-create → API-key-issue → SDK-verify-loop. (J1) full-happy-path-through-cockpit + (MK-COLOC) marketing colocated in cockpit Next.js app + (O1) auto-create-org-on-first-OAuth + (K1) API-key-visible-once + (T2) cockpit polls `last_used_at` for in-cockpit SDK verification confirmation. Five rejected alternatives with named winning conditions: (J2) CLI-augmented (no MVP CLI); (J3) auto-everything (contradicts (L1) 2-person-team multi-project need); (T1) silent SDK call (forfeits onboarding-confidence loop for ~30 LOC savings); (O2) explicit "Create Organization" step (extra friction; Better Auth defaults to auto-create); (K2) always-visible API key (DB compromise = leak). Three failure modes with mitigations: step-6 mid-transaction failure (wrap in `withTenant` transaction, project pattern); step-11 polling never fires (5-minute timeout + self-service docs exit); OAuth provider downtime (clear error + email/password fallback per (iii)). Six revisit-when triggers. **Domain strategy implied:** `bokchoy.com/` apex = cockpit (marketing route + auth-gated routes via Next.js route groups); `api.bokchoy.com/` = backend Hono per `[[cockpit-stack-integration-research]]` F1 reverse-proxy. Cascade obligations for slice 8.4 (project-create handler + API-key issuance) + slice 8.3 verify-key polling view + `[[cockpit/admin-list-endpoints-contract]]` (`POST /v1/projects` + `GET /v1/projects/{id}` shape contracts). **`[[cockpit-shape]]` amended** in same turn: marketing/landing colocation row added to *Decision*; 3 revisit-when triggers added for marketing-workspace split (>1 page scope OR marketing-team hire OR performance regression).
- [[cockpit-shape]] — type: decision — **Cockpit MVP scope, IA, project lifecycle, success criteria, and non-goals RESOLVED 2026-05-11.** (M2) MVP scope cut = **dashboard v1** per `[[mvp-feature-sequence]]` months 1-3 sequencing: sign-in/sign-up (OAuth Google+GitHub primary + email/password fallback per design pick (iii) 2026-05-11) + projects list + project detail + project creation flow (auto-issues first SDK API key) + catalog editor + transaction inspector + player search + API-key issuance/revocation + bootstrap-reason-codes action. **(L1) Cockpit creates projects via UI button** — production-cited × 4 (Stripe Dashboard / Vercel Dashboard / Linear / Resend). Implies new backend handler `POST /v1/projects` behind `adminGate({ resource: 'project', actions: ['create'] })`. **(I1) Left sidebar nav** — production-cited × 4 (same Stripe/Vercel/Linear/Resend convergence at indie/SMB-MVP-team-size class). Top-level sections: Projects / Catalog / Transactions / Players / Settings. Active-project switcher in header. Sidebar designed with collapsible-groups capacity from day 1 to defer re-architecture at cockpit-core (slices 8.7+). **Slice sequencing within months 1-3 dashboard v1 target:** 8.3 (β: sign-in + projects list + bootstrap-action; 3-4 cockpit pages + 3 backend endpoints — Better Auth handler mount + `GET /v1/orgs/me` + `GET /v1/projects`) → 8.4 (project creation + API-key issuance) → 8.5 (catalog editor CRUD via M2 stored-functions per `[[catalog-versioning]]`) → 8.6 (transaction inspector + player search). Slices 8.7+ are cockpit-core (months 4-5; deferred). **Success criteria:** β-slice = sign-up via OAuth + see projects + trigger bootstrap-reason-codes end-to-end; dashboard v1 = full MVP cockpit + API-key issuance + first authenticated SDK call; first-paying-customer = designer can run live-ops without engineer involvement (validates cockpit-core gate). **10-row Non-goals table (explicit OUT with named revisit-when):** cockpit-core (months 4-5), battle pass/quest/mailbox UI (month 6), AI tooling (month 7), VIP/whale first-class surface, multi-team-member workflows (single-user-per-org MVP), cross-project portability, webhook config UI, `walletDeidentifyPlayer` UI (blocked on Q3 DSR research), designer-facing live-ops surfaces (Hiro-style), self-service billing. **Five rejected alternatives** with named winning conditions: (M1) β-only-shape (wins only if spine slips), (M3) cockpit-core-at-MVP (rejected — contradicts `[[mvp-feature-sequence]]` Alternative-B rejection on no-gaps constraint), (L2) auto-create-default-project (inconsistent UX), (L3) out-of-band-only (rejects 2-person-team affordance expectation), (I2)/(I3) tabs/route-segments-only (overflow + no nav context). **Three failure modes named with mitigations:** primary scope-creep-into-cockpit-core during 8.4-8.6 (mitigation: explicit non-goals enforcement at PR review; every cockpit feature must trace to dashboard v1 scope item OR amendment); secondary sidebar IA pre-commit not fitting cockpit-core extension (mitigation: design with `sections` prop accepting collapsible groups from day 1); tertiary 2-person-team audience not generalizing (mitigation: queue customer-discovery question for first design-partner). **Six revisit-when triggers** named for the decision.
- [[cockpit-stack-integration-research]] — see `## architecture` section (cross-cutting research entry).

## architecture

- [[idempotency-strategy]] — type: decision — resolves CL-028. **Rewritten 2026-05-01 per `[[idempotency-strategy-research]]`.** Hybrid keys (server-derived natural + client-supplied `Idempotency-Key` header), Postgres-only storage (B7, no Redis), per-step UNIQUE + outbox (`staged_jobs`) + deterministic-RNG-on-key for loot (D2-α, no recovery-point state machine), 422 parameter-mismatch / 409 in-flight per IETF draft 07 + Stripe SDK behavior, bokchoy-prefixed error codes, 24h TTL Shopify-cited. Cascade obligations: `[[CL-029]]` defines outbox schema; `[[CL-031]]` defines RNG seed format; `[[runbook-idempotency]]` written in implementation phase.
- [[idempotency-strategy-research]] — type: research — sense-check of `[[idempotency-strategy]]` + Round 2 closing D2 (multi-step recovery). Tier-1 evidence on Brandur via `rocket-rides-atomic` schema; AWS + microservices.io tier-2 on outbox pattern. Surfaces **D2-α: per-step idempotency keys + outbox, no recovery-point state machine** — simpler than Brandur's full pattern, sufficient for BokChoy MVP (no expensive non-idempotent steps), upgrade path to D2-β preserved per-endpoint. Recommendation: B7 (Postgres-only, Path α) + D2-α + deterministic-RNG-from-key for loot + outbox for side effects. Outbox schema and RNG specifics deferrable to CL-029/CL-031 respectively.
- [[wallet-source-of-truth-research]] — type: research — first half of CL-029. Triangulated across Stripe Ledger (path A, fintech scale), PlayFab Economy v2 + Beamable + LootLocker + AWS-QLDB-reference (path B / D, game-backend scale), and Brandur (path B mechanics, already vaulted). All four named game-backends ship CRUD-on-balance with optional separate audit log (path B); Stripe is the only path-A reference and operates under regulatory + 5B-events/day conditions BokChoy does not face. Contradiction probe (Dudycz, Doomen) catalogues path-A's operational taxes. Path D (CRUD interface + ledger storage) unavailable on Postgres without additional infra. **Errata 2026-05-02:** trigger-mechanism recommendation was inferred; superseded by `[[wallet-audit-invariant-research]]`.
- [[wallet-audit-invariant-research]] — type: research — answers Q1 of the three CL-029 follow-ups. Investigates how production Postgres ledger systems enforce paired-write invariants between balance and audit rows. Findings: NO surveyed production reference uses triggers for this purpose. GitGuardian published documented trigger-removal post-mortem. Cross-row CHECK constraints eliminated as candidate (concurrency-unsafe per Postgres community). **Erratum 2026-05-04 per `[[wallet-functions-research]]` source-walk:** S3's pgledger SECURITY-DEFINER + only-through-functions claim falsified — pgledger ships M1 (zero `SECURITY DEFINER`/`GRANT`/`REVOKE`/`CREATE ROLE`, direct `UPDATE pgledger_accounts` shown as normal usage in `examples/lock-account.sql`). F2 + F3 corrected. **Cite cluster after correction:** M1 production-cited tier 1 across Brandur + Square Books + pgledger (three independent ledger references). M2 docs-cited tier 2 (Postgres canonical SECURITY DEFINER + REVOKE/GRANT) with no production cite for ledger code in surveyed sources. M3 anti-pattern unchanged. Per *Source quality ladder*, tier-1 M1 beats tier-2 M2 — `[[wallet-mechanics]]` Amendment 2026-05-04 A1 downgrades §2 from M2 to M1 accordingly. Hands back to `/design` for M1/M2/M3 pick (resolved 2026-05-04 → M1).
- [[staged-jobs-schema-research]] — type: research — answers Q3 of the CL-029 follow-ups. Brandur's minimal `(id, job_name, job_args)` is **insufficient** as a sole production-cite for outbox schema; supplemented with pg-boss + pg-transactional-outbox + Patel's reference for full claim/retry/dead-letter shape. Schema lands: status enum (CHECK-constrained), retry attempt + exponential backoff via `scheduled_at`, dead-letter via `status='dead'`, partial index on pending rows, SKIP LOCKED for worker claim. **Loot-roll organization decided:** unified `transactions` audit table (path-B per `[[wallet-source-of-truth-research]]`) + sister tables (`loot_rolls`, `iap_receipts`) for domain-specific data, joined via polymorphic `related_id` + `related_type`. Cascades to CL-031 (`loot_rolls` schema). Recommendation lane (research surfaces, design chooses): `staged_jobs` schema in F1 + sister-tables in F4 + mechanism M2 from `[[wallet-audit-invariant-research]]`.
- [[audit-retention-research]] — type: research — answers Q2 of the CL-029 follow-ups. **No surveyed regulator imposes a per-transaction retention floor on F2P virtual currency** (UK GC explicitly does not regulate loot boxes, PEGI 2026 disclosure is catalog-level not transaction-level, ESRB declined monetisation rules). Operational floor set by **card-scheme chargeback windows: 540 days under reason code 13.1**. Industry pattern (Supercell, King): retain indefinitely under legitimate-interest basis, de-identify periodically. GDPR right-to-be-forgotten reconciled via de-identification (preserve transaction shape, hash player_id, drop PII fields). Storage math at MVP scale: ~22M-220M rows/year indie, ~1.8B-18B rows/year Studio+ — **monthly partitioning on `created_at` from day 1 via pg_partman**. Three-tier model: hot Postgres (24 months) → cold S3 Parquet (24-60 months) → Glacier archive (indefinite). Confirms PCI-DSS out of scope (BokChoy doesn't process PAN, Apple/Google do).
- [[wallet-mechanics]] — type: decision — resolves CL-029. **Amended 2026-05-04 (Part 3)** per `[[idempotency-keys-schema-research]]` + `[[economy-primitives-research]]`: A14 D11 picks (JSONB request_params over fingerprint; NULL-until-locked over Brandur DEFAULT now()); A15 D12 pick R3 per-project reason-codes allowlist over R2 open-TEXT — currencies/wallets/reason_codes schemas cross-referenced from research entries (F3/F4/F6); A16 §3 `transactions` gains `wallet_version BIGINT NOT NULL` column (pgledger forensic-version pattern) + composite FK on `(project_id, reason_code) → reason_codes`; A17 cascade obligations extended (CI lint adds new tables; Drizzle schemas; bootstrap 12-code default-set on project creation; staged_jobs CHECK adds `idempotency_reaper`; SDK auto-key generation `bokchoy-sdk-retry-${uuid4()}`); A18 BCxxx allocations extended (BC050 ReasonCodeNotRegistered, BC060 CurrencyNotFound; reserved BC000-BC099 for wallet/inventory/idempotency primitives). **Amended 2026-05-04 (Part 2)** per `[[tenancy-ids-research]]` (Q1+Q2 of wallet research queue): A8 §3 `transactions.player_id` BIGINT→UUID; A9 §5 `loot_rolls.player_id` + `iap_receipts.player_id` BIGINT→UUID + cascade to `[[loot-rng-construction]]` seed-format `player_id 8B → 16B`; A10 §6 `wallet_deidentify_player(BIGINT)` → `(UUID)` + HMAC→bigint projection rewritten as deterministic-UUIDv8 projection per RFC 9562 §5.8 (122 effective entropy bits, 5×10⁻²⁰ collision at 1B players, spec-correct shape for app-defined deterministic UUIDs); A11 *Revisit when* 10M-player trigger retired (functionally unreachable at v8 width); A12 cascade obligations extended (Better Auth `advanced.database.generateId: "uuid"` config setting REQUIRED — without it the entire RLS GUC `current_setting('app.current_tenant')::UUID` cast breaks; Drizzle auth-schema hand-translation per `[[backend-stack]]`); A13 **UUIDv4 today, defer UUIDv7** — `gen_random_uuid()` Postgres-native + `crypto.randomUUID()` Node stdlib; revisit when Supabase managed ships Postgres 18 native `uuidv7()` OR sustained writes >100/sec/project AND Postgres 18 not shipped (whichever first); UUIDv7-today via pgledger's vendored SQL helper vaulted as rejected alternative with named cognitive-overhead winning condition. **Amended 2026-05-04** per `[[wallet-functions-research]]` (pgledger source-walk falsified M2 cite chain): A1 §2 mechanism downgraded M2 → M1 (production-cited tier-1: Brandur + Square Books + pgledger all ship M1); A2 line 466 concurrency SERIALIZABLE → READ COMMITTED + FOR UPDATE (pgledger pattern, simpler call sites, predictable lock duration, no caller retry budget); A3 line 468 OTel-from-plpgsql → three-layer realistic mechanism (TS-side OTel span + plpgsql `RAISE LOG` + `pg_stat_statements`); A4 §3 single-vs-double-entry deliberate-divergence vaulted (virtual currency conservation regime ≠ real-money ledger); A5 BCxxx SQLSTATE namespace pinned (BC001-BC030 for wallet primitives); A6 cascade obligations updated (CI lint replaces GRANT-migration scan; OTel + SQLSTATE conventions added); A7 Failure mode 1 amended to M1-class bypass (CI lint mitigates). M1→M2 upgrade migration preserved as future option triggered by team-growth past 5 engineers OR GRANT-migration drift incident OR compliance regime demand. **Amended 2026-05-02** per `[[webhook-retry-norms-research]]` + `[[deidentify-mechanism-research]]` + `[[multi-tenant-rls-research]]` + `[[host-platform]]`. Combined entry covering: path B wallet source-of-truth (CRUD on balance + same-txn audit log); M2 enforcement (stored-function-only-interface, app role has EXECUTE-only on `wallet_credit`/`wallet_debit`/etc., no UPDATE on protected tables); unified `transactions` audit log with sister tables (`loot_rolls`, `iap_receipts`) joined via polymorphic `related_id`+`related_type`; `staged_jobs` outbox schema with per-kind max_attempts encoding **operator-posture choices** (8/5/5/3 with named policy comments); §4a list-failed-deliveries API + §4b per-subscription failure-rate limiter + §4c dead-letter retention ≥7d as **required deliverables for the short-window webhook posture**; 24-month hot + S3 cold + Glacier archive retention with monthly partitioning via pg_partman; **§6 de-identification rewritten — HMAC-SHA-256 via pgcrypto replacing MD5, per-transaction `SET LOCAL bokchoy.anon_secret` from secrets manager, BIGINT-projection collision math documented, key-destruction = GDPR-recognized erasure, no rotation by default**; **§8 NEW — cross-tenant isolation via Postgres RLS, non-owner `bokchoy_app` role, `FORCE ROW LEVEL SECURITY` on every protected table, per-request `SET LOCAL app.current_tenant` from middleware, partition pruning preserved under BokChoy's shape, real perf risk is leakproof regression on inner predicates**. Names twelve failure modes (1-7 original + 8 RLS leakproof + 9 RLS owner-bypass + 10 RLS context-bleed + 11 de-id-secret-missing + 12 webhook-auto-deactivation-false-positive). PlanetScale app-layer-enforcement vaulted as named rejected-alternative; v1's MD5 construction also vaulted as superseded. Cascade obligations expanded to nine items including CI lint for RLS presence + `pg_partman`/Supavisor verifications inherited from `[[host-platform]]`.
- [[catalog-versioning-research]] — type: research — first pass on CL-030. **DESIGN.md §12.3's "DAG semantics, branchable" framing is not production-cited** — falsified across PlayFab Catalog v2, Beamable Content, Unity GS Economy, LaunchDarkly, Statsig, Contentful, Sanity. No surveyed system ships Git-style branch+merge inside the catalog. Five distinct production-cited patterns surface: state-based (PlayFab Draft/Published), multi-environment promotion (Unity Dev/Staging/Prod, Beamable namespaces, Contentful aliases), linear history with rollback (LaunchDarkly, Sanity), Configuration-as-Code with external git (Unity), approval workflows + scheduled deploys (LaunchDarkly Workflows). JSON Patch (RFC 6902) and JSON Merge Patch (RFC 7396) are wire formats only — neither resolves concurrent-edit conflicts; that's solved at the application layer via ETag optimistic locking + process discipline. Hands four-tier menu to design: T1 minimum-viable (PlayFab-shape, MVP scope), T2 ergonomics layer (bulk publish, approval workflows), T3 CaC upgrade (Unity-style git-driven), T4 out-of-scope (real-time CRDT, atomic env swap, row-level DAG).
- [[catalog-versioning]] — type: decision — resolves CL-030. T1 + T2-subset shipped at MVP: per-item Draft/Published/Archived state machine; ETag optimistic concurrency via `version` integer; `catalog_items` table with type discriminator (item/currency/bundle/store/subscription/loot_table); linear `catalog_audit` log with JSON Patch (RFC 6902) diffs, monthly partitioned but no cold-tier needed at MVP volume; M2 stored-function-only-interface transferred from `[[wallet-mechanics]]` (`catalog_create_draft`, `catalog_update_draft`, `catalog_publish`, `catalog_publish_bulk` with optional `scheduled_at`, `catalog_unpublish`, `catalog_archive`). Per-project environment isolation (no `environment` column) consistent with `[[idempotency-strategy]]` scoping. Deferred T2 items: approval workflows (no MVP customer signal; T3's git PR is the parallel approval surface). Cascades: CL-031 loot tables follow this lifecycle; `[[catalog-cac-upgrade]]` stub captures T3 upgrade path.
- [[catalog-cac-upgrade]] — type: exploration — **deferred stub** for the T3 Configuration-as-Code upgrade path (Unity GS Economy pattern). Not built at MVP; conditions for revisiting recorded (paying customer demand, cross-project promotion friction, team-size growth past 5 concurrent contributors, compliance mandate). Reuses T1+T2 primitives from `[[catalog-versioning]]`; no new schema. Promoted to full decision entry when revisit conditions trigger.
- [[webhook-retry-norms-research]] — type: research — sense-check on `[[wallet-mechanics]]` §4 `webhook_fire = 8` value. Surveys Stripe (3 days, ~16 attempts), Shopify (4 hours, 8 attempts; tightened from 19/48h on 2024-09-10), Twilio (sub-call, 0–5 default 1), GitHub (zero auto-retry, 3-day manual replay), Slack (3 retries, ~6 min). Modal cluster for asynchronous business events is hours-to-days. BokChoy's 4.3-min/8-attempt draft sits at the SHORT end. Gusto Embedded 2025 retry-storm post-mortem corroborates that long retry tails generate cross-tenant noisy-neighbor failures in multi-tenant systems — validates BokChoy's posture but conditional on (a) list-failed-deliveries API, (b) per-subscription failure-rate limiter, (c) dead-letter retention ≥7 days. None of these three currently named in `[[wallet-mechanics]]`. Hands back to `/design` for the amendment.
- [[deidentify-mechanism-research]] — type: research — sense-check on `[[wallet-mechanics]]` §6 de-identification function. Resolves three load-bearing claims: (1) MD5(message||key) is rejected — collision-resistance broken (Wang 2004), Preneel-van Oorschot 1995 envelope critique on suffix-keyed hashes, Wikipedia/length-extension on the construction class. HMAC-SHA-256 via pgcrypto is the production-cited replacement (Elastic engineering post, named authors). (2) `bokchoy.anon_secret` namespaced GUC is documented Postgres syntax, not just convention. (3) `SET LOCAL` + PgBouncer transaction-pool is documented-safe — the Postgres SET docs confirm transaction-scoped lifetime; PgBouncer features.html marks `SET/RESET` "Never" but does NOT list `SET LOCAL` as incompatible; backend is released on commit/rollback, GUC gone. Don't use `connect_query` with plain `SET` — leaks across pooled clients. Concrete pattern: `hmac(convert_to(player_id, 'UTF8'), convert_to(key, 'UTF8'), 'sha256')` returning bytea, `SECURITY DEFINER` + `SET search_path = pg_catalog`, app issues `SET LOCAL` per transaction reading from AWS Secrets Manager / Vault. Key destruction = GDPR-recognized erasure (AEPD/EDPS PDF cited; verbatim quotes require local fetch).
- [[host-platform]] — type: decision — Supabase managed Postgres at MVP, **DB-only** (no Auth, Storage, Realtime, Edge Functions, PostgREST). Cost is the reason — Supabase free tier is the cheapest legitimate managed Postgres for MVP traffic. DB-only is the lock-in mitigation: no Supabase-specific helper used = migration is a connection-string change. Symmetric to `[[idempotency-strategy]]` B7's storage-portability commitment. Two cascade obligations: (1) verify `pg_partman` availability before implementation; (2) verify Supavisor transaction-mode supports `SET LOCAL`. Five rejected alternatives with named winning conditions. Five failure modes with mitigations.
- [[within-roll-composition-scope]] — type: decision — **CL-031 sub-decision (iii.b) resolved + CL-031 closes.** P2-strict chosen: within-roll composition (no-duplicates-within-bundle, slot-by-slot composition, slot guarantees) is NOT a BokChoy platform primitive; customer composes K calls in extension code with optional exclusion-set + retry pattern. BokChoy ships single-item-per-call `loot_roll` + reference composition impl as docs artifact (extension to `[[pity-engine-scope]]` F1 reference). Defense: 4-platform survey was contested (2-of-4 ship platform-level no-dup, 2-of-4 don't); P2-strict matches `[[pity-engine-scope]]` posture; customer code is ~10 lines. Rejected P2-with-helper (`loot_roll_excluding`) and P2-with-config (`max_repeats_within_roll` on `loot_table` — opens "roll session" platform concept). Three failure modes named with mitigations. **CL-031 fully resolved.**
- [[loot-rng-output-research]] — type: research — **CL-031 sub-decision (iii) research pass.** (iii.a) rejection sampling **universal** — libsodium `randombytes_uniform` + OpenBSD `arc4random_uniform` both use rejection with bound `(2^32 mod weight_sum)`; modulo bias explicitly rejected in both implementations' comments. (iii.c) **stream from one HMAC_DRBG instance per `loot_roll`** — go-hmac-drbg Generate() is stream-mode; 9999-call ceiling per Instantiate is non-binding for BokChoy's per-call seed model. (iii.b) **convergent on slot-by-slot composition + customer-code orchestration** under P2; PlayFab/Hearthstone slot-composition pattern; Hiro/AccelByte ship within-roll **no-duplicates** at platform level (`max_repeats`, `max_repeat_rolls`, AccelByte's "never rewarded the same item"). **New design fork surfaced:** P2-strict (customer code does within-roll constraints) vs. P2-with-helper (`loot_roll_excluding`) vs. P2-with-config (`max_repeats_within_roll` on `loot_table`). Triangulation met across 4 platforms + 2 cryptographic libraries; confidence medium — NIST SP 800-90A Rev.1 PDF direct-fetch failed (binary rendering issue), production-code cites stand in as implementation-level substitutes per the ladder. Open: NIST PDF render, AccelByte reference-impl source for platform-vs-example clarification, console-AAA composition patterns.
- [[loot-rng-construction]] — type: decision — **CL-031 sub-decision (ii) resolved.** PRF: HMAC_DRBG (NIST SP 800-90A) over HMAC-SHA-256 via HashiCorp `go-hmac-drbg` or equivalent. Server secret enters as HMAC_DRBG `entropy_input` (per RFC 6979 §3.3). Canonicalization: hybrid A+B — fixed-length binary for `(project_id 16B, banner_id 8B, player_id 8B, attempt_number 4B)` + 4-byte length-prefix for `pull_session_id` UTF-8. Rotation: α+β-default — new `rng_key_id SMALLINT` column on `loot_rolls`, server-populated from GUC, default policy "no rotation by default" symmetric to `[[deidentify-mechanism-research]]`. Secrets: `bokchoy.rng_secret` and `bokchoy.anon_secret` independent (Krawczyk key-separation axiom). Five rejected alternatives named (pure A/B/C, HKDF, BIP-32-chained, pure-β, shared-secret). Five failure modes named with mitigations. **Cascade:** `[[wallet-mechanics]]` §5 schema patch (add `rng_key_id` column); `[[runbook-idempotency]]` rotation + deployment-check runbook items; pre-implementation security review of canonicalization function; reference pity impl now unblocked. Sub-decision (iii) PRF-output → roll mapping is next.
- [[loot-rng-research]] — type: research — CL-031 sub-decisions (ii) seed format + PRF construction + (ii.4) rotation-vs-replay-determinism. **PRF: HMAC_DRBG (NIST SP 800-90A) instantiated with HMAC-SHA-256** — production-cited via HashiCorp `go-hmac-drbg`, RFC 6979 reference design, stack-consistent with existing HMAC-SHA-256 commitment. **Server-secret usage: as `entropy_input` to HMAC_DRBG**, NOT HKDF (wrong primitive class for per-call determinism), NOT BIP-32 chain-code (hierarchical pattern unnecessary for flat shape). **Seed canonicalization: three production-cited patterns surfaced (Pattern A fixed-length binary BIP-32, Pattern B length-prefix csexp, Pattern C canonical CBOR/JSON), no clean winner — selection conditional on `pull_session_id` shape; hybrid A+B mirrors RFC 6979's int2octets+bits2octets composition.** **Rotation: Pattern α (key-id stamp + version-aware lookup) recommended-for-design** — production-cited via SaaS Shield Deterministic Encryption; symmetric to existing `anon_key_version` pattern. Pattern β (no-rotation, accept invalidation per BIP-32 stance) is the alternative. Open: NIST SP 800-90A direct fetch, RFC 8949 §4.2 direct fetch, security review of chosen canonical form (no specific forge-attack post-mortem found in the public record — class-level OWASP/CWE only). Cascade for design: decide whether `bokchoy.rng_secret` and `bokchoy.anon_secret` share or differ. Sub-decision (iii) PRF-output → roll mapping deferred.
- [[pity-engine-scope]] — type: decision — **CL-031 scope decision.** P2 chosen: pity is NOT a BokChoy platform primitive; customer ships pity in extension code. BokChoy ships `loot_roll` server function + `loot_rolls` audit table with **opaque** `pre_state`/`post_state` JSONB blobs (renamed from `pity_state_before/after`) + reference impl as docs artifact. **Cascade obligations:** patch `[[wallet-mechanics]]` §5 column names; cancel `[[catalog-versioning]]` `pity_class` cascade from `[[pity-state-research]]`; CL-031 (i)/A/B/C is MOOT at platform level; (ii) seed/PRF + (iii) RNG-output mapping remain LIVE. Rejected P1 (Hiro-stance built-in pity, schema lock-in + monetization-lever ownership mismatch) and P3 (hybrid, MVP team can't run two surfaces). Four failure modes named with mitigations.
- [[pity-state-research]] — type: research — first pass on CL-031 sub-decision (i). Three platform stances surveyed (Hiro built-in / AccelByte Extend Override / PlayFab Cloud Script). Per-banner-TYPE carry-over is the modal pattern across surveyed 2024–2026 gacha (Genshin, HSR, Neverness); Seven Deadly Sins Origin ships per-banner-reset. Position A gains a production cite (Hiro). Position B (derive from `loot_rolls` event log) loses ledger-by-analogy support — no surveyed game-backend platform applies event-sourcing to pity. Keying is the load-bearing question, independent of A/B/C: state needs `(player_id, banner_type)`, NOT `banner_id` and NOT just `player_id`. Cascade obligation if design proceeds: `[[catalog-versioning]]` `loot_table` schema must expose a `banner_type` / `pity_class` field.
- [[multi-tenant-rls-research]] — type: research — fills the cross-tenant isolation gap in `[[wallet-mechanics]]`. RLS is the dominant published pattern in non-Supabase guidance (AWS Prescriptive Guidance verbatim "required to maintain tenant data isolation in a pooled model"); Crunchy Data, Heroku, Drizzle, Prisma all teach the same pattern. Strongest named counter-position: PlanetScale / Simeon Griggs 2026-04-21 ("we generally don't recommend relying on RLS"). Canonical pattern: `SET LOCAL app.current_tenant = '<uuid>'` (or `set_config(name, val, TRUE)`) per transaction; policy reads `current_setting('app.current_tenant')::UUID`. PgBouncer transaction-pool + `SET LOCAL` is documented-safe (Postgres SET semantics + PgBouncer transaction-mode rule + Heroku prescriptive guidance + JP Camara independent confirmation). Partition pruning NOT at risk under BokChoy's shape (created_at partition key + project_id RLS column are orthogonal; empirical: scalar `current_setting()::int` against partition column prunes correctly per pgsql-hackers Marcelo Zabani 2024-08-07). Real perf risk is leakproof-ness regression on inner predicates (Pierre Ducroquet pgsql-hackers 2019), not pruning loss. Three RLS-specific failure modes named: leakproof regression (8a), owner bypass (8b), context-bleed (8c). No public production post-mortem at ≥1k tenants / ≥10k rps was found — at BokChoy's potential scale the case studies don't exist. Hands back to `/design` for the amendment.
- [[tanstack-query-rsc-research]] — type: research — answers TanStack Query + Next.js 16 RSC integration question. **Q1 canonical pattern (verbatim from official docs):** Server Component prefetches via `await queryClient.prefetchQuery({queryKey, queryFn})` → wraps Client Components in `<HydrationBoundary state={dehydrate(queryClient)}>` → Client Components consume via `useQuery`. **QueryClient lifecycle:** per-request via React's `cache(() => new QueryClient(...))` to prevent cross-request leakage. **Critical staleTime config:** default is 0 — MUST set `defaultOptions.queries.staleTime: 60_000` minimum (per TanStack Query docs verbatim) to prevent client-side refetch immediately after RSC hydration. Per-query staleTime: live-ops 5s, slow-changing 5min, static 1hr. **Q2 Server Actions + invalidation derived pattern (gap-as-finding):** surveyed scope does NOT document Server Actions + TanStack Query + revalidateTag triple-invalidation. Pattern derived from orthogonal layer composition: Server Action mutates DB + calls `revalidateTag(tag)` (Layer 1, Next.js Cache Components); client `useMutation.onSuccess` calls `queryClient.invalidateQueries({queryKey})` (Layer 2, TanStack Query client cache). Both layers fire on mutation; orthogonal responsibilities. **Tkdodo (TanStack Query maintainer) canonical default: broad invalidation via global `MutationCache.onSuccess` + ~2min staleTime; tighten to fine-grained per-key only if profiling shows over-invalidation.** **Q3 TanStack Table v8 + Virtual:** production-grade on Next.js 16 (Admindek 35+ shadcn primitives + TanStack Table v8 server-side pagination; Apex SaaS template — subscription analytics + user management via TanStack Table v8). Server-side pagination via `manualPagination: true` + `queryKey: ['table-data', {page, sort, filter}]`. Client Components wrapping virtualized rows. **Q4 anti-patterns:** (a) TanStack Query owns ALL fetching — forfeits RSC streaming + Cache Components; (b) `fetchQuery` results rendered server-side — causes hydration sync issues per official docs verbatim caveat ("don't render its result on the server or pass the result to another component"); (c) staleTime=0 default — double-fetch on hydration; (d) double `invalidateQueries` synchronously causes double refetch (Issue #7963). **Triangulation:** ✓ canonical pattern (TanStack Query docs verbatim) + Tkdodo invalidation guide + Admindek/Apex production templates + Issue #7963 contradiction probe. Confidence: high overall; medium on Q2 derived triple-invalidation pattern (gap-as-finding, needs first-paying-customer-launch validation). **Hands back to /design** to amend `[[frontend-stack]]` with TanStack Query + Table + Virtual integration patterns + the four supplementary picks (shadcn/Radix/Tailwind, Playwright/Vitest, Render, Zod/RHF).
- [[frontend-stack]] — type: decision — **frontend stack RESOLVED + AMENDED 2026-05-03 (continuation) with five supplementary picks + TanStack integration patterns.** Original picks: Next.js 16 + Vercel + Bun runtime + Server Actions + Cache Components + Bun workspaces + Turborepo. Amendment adds: **shadcn/ui + Radix UI + Tailwind v4** (UI library, Admindek/Apex production cites); **Playwright (E2E) + Vitest (unit)** (testing — Vitest over `bun test` for ecosystem maturity, runs cleanly on Bun); **Render** for backend container PaaS (existing-subscription resource constraint, also amends `[[backend-service-shape]]`); **Zod v4 + React Hook Form + `@hookform/resolvers/zod`** with single-source-of-truth schemas in `packages/shared-types/`; **TanStack ecosystem (Query + Table + Virtual)** with derived integration patterns from `[[tanstack-query-rsc-research]]` — RSC `prefetchQuery` → `<HydrationBoundary state={dehydrate(queryClient)}>` → client `useQuery`; per-request QueryClient via React `cache()`; `defaultOptions.queries.staleTime: 60_000` minimum (per TanStack Query docs verbatim); per-data-type staleTime presets (live-ops 5s / slow-changing 5min / static 1hr); **Server Actions + invalidation triple-pattern (derived gap-as-finding):** Server Action mutates DB + `revalidateTag(tag)` (Layer 1, Next.js cache); client `useMutation.onSuccess` calls `queryClient.invalidateQueries({queryKey})` (Layer 2, TanStack Query cache); **Tkdodo broad-invalidation default at MVP** (global `MutationCache.onSuccess`); **TanStack Table v8** for cockpit tables with `manualPagination: true` + server-side pagination via `queryKey: ['table-data', {page, sort, filter}]`; **TanStack Virtual** for long lists (transaction history, audit log). Anti-patterns enforced via PR review/CI lint: ban `fetchQuery` server-side rendered; require explicit `staleTime` config; 'use client' leaf-placement; double-invalidation (Issue #7963) avoided. **Original entry RESOLVED.** Next.js 16 (currently 16.2; pin 16.0.7+ at MVP launch per CVE-2025-55182 patch) + App Router + React 19.2 + Turbopack default + React Compiler 1.0. **Deploy: Vercel for cockpit + container PaaS for backend (split deploy, industry-standard pattern, Cal.com cite).** **Cockpit runtime: Bun on Vercel (Public Beta) — user explicit risk acceptance** with F-Cockpit-Bun-1/2/3 + cross-runtime discipline (no `Bun.*` APIs, ESLint blocks `import 'bun'`) + 1-hour `vercel.json` config-flag fallback to Node 22. **Mutations: Server Actions for cockpit + Route Handlers for SDK API (settled 2026 production pattern). Better Auth integration:** `auth.api.getSession({ headers: await headers() })` in RSC + `nextCookies()` plugin + `proxy.ts` for route-level auth (Next.js 16 middleware replacement). **Caching: Cache Components opt-in (`cacheComponents: true` + `"use cache"` directive) — live-ops uncached/per-request inside Suspense; slow-changing data cached with `cacheLife` profiles; sub-50ms TTFB on personalized routes.** **Bundle discipline:** `'use client'` at leaf components only (cascade documented at Next.js Discussion #13763 / Issue #60246), no wildcard imports, `optimizePackageImports` config, `@next/bundle-analyzer` in CI, <200KB gzipped initial JS target. **Monorepo: Bun workspaces + Turborepo hybrid (OpenCode production cite for the exact pattern, 20+ packages).** Workspace structure: `apps/{backend,cockpit}/` + `packages/{db,shared-types,auth-config}/` (+ `packages/ui/` deferred). Bun catalog for shared dependency version pinning. Turbo task graph for cross-workspace builds. **CVE discipline:** Renovate/Dependabot auto-PR for patches; `bun audit` in CI; security advisory subscriptions; 24-hour critical patch SLA. **Ten rejected alternatives** (SPA+Vite, Remix, SvelteKit/Solid Start, Astro, container-PaaS-for-cockpit, Cloudflare Pages, Node 22 on Vercel as conservative pick rejected via user risk-acceptance, Pages Router, pure Bun workspaces without Turbo, pnpm workspaces+Turbo). **Nine failure modes** with mitigations (F-RSC-1 future RSC CVE class, F-Cockpit-Bun-1/2/3 Vercel runtime gaps, F-Frontend-1 'use client' cascade, F-Frontend-2 split-deploy CORS/cookie/region, F-Frontend-3 Server Action validation gap, F-Frontend-4 cache staleness, F-Frontend-5 turbo-prune-Bun gap, F-Frontend-6 Bun catalog gap). **Fifteen cascade obligations** for implementation phase. **All session-arc architecture decisions now closed:** auth + compliance, language + ORM + auth-library + runtime, service shape, frontend stack, monorepo tooling.
- [[bun-workspaces-research]] — type: research — answers monorepo-tooling open thread from `[[frontend-stack-research]]`. **Critical framing fix: "Bun workspaces vs Turborepo" is a category error — they compose at different layers.** Bun workspaces = Layer 1 (package manager + workspace dep resolution); Turborepo = Layer 2 (task orchestration + caching + parallelization on top of any package manager). Turborepo 3.0 ships first-class native Bun workspace detection (`bun.lockb` / `bun.lock`). **Production cite for the hybrid: OpenCode (anomalyco/opencode + sst/opencode) — 20+ packages in monorepo using Bun workspaces + Turbo for build orchestration + Bun catalog for shared dependency version pinning + `workspace:*` protocol.** Trigger.dev uses Turborepo for build orchestration (with Bun runtime in some workspaces); Cal.com uses Turborepo (per `[[frontend-stack-research]]`); Autumn (useautumn) uses Bun workspaces (3-tier monorepo). **Important nuance correcting earlier OpenCode cite in `[[backend-service-shape-research]]` Source 4:** Jay V's "moving to Node and Electron, away from Bun and Tauri" was specifically about desktop app runtime stack (Tauri→Electron, Bun-as-Electron-runtime→Node), NOT monorepo tooling. OpenCode retains Bun workspaces + Turbo for monorepo even while moving desktop runtime away from Bun. Package-manager-vs-runtime layer split confirmed. **Industry consensus 2026 stack per untergletscher.com: "Bun replaces npm/pnpm + Vitest/Jest + Node.js, Biome replaces ESLint/Prettier, Turborepo orchestrates everything with zero-config caching." Single-tool, multiple-roles philosophy.** **Edge cases / contradiction probes:** `turbo prune` Bun support gap (vercel/turborepo Discussion #7456 — affects slim-image Docker builds); `bun link` edge cases vs pnpm workspace protocol; pnpm catalog→Bun catalog migration friction (Issue #14662); shadcn-ui Discussion #6465 documents Turborepo+Bun script issues. **Recommendation: Bun workspaces + Turborepo hybrid.** Workspace structure: `apps/{backend,cockpit}/` + `packages/{db,shared-types,auth-config,ui}/` matching `[[backend-service-shape]]` module decomposition. Catalog for shared dependency pinning. Turbo task graph for cross-workspace builds. Vercel deploys cockpit via `turbo run build --filter=cockpit` + Bun runtime. **Hands back to /design** for `[[frontend-stack]]` vault (now unblocked) with monorepo tooling resolved.
- [[frontend-stack-research]] — type: research — answers Q1 Next.js 16 production-readiness + Bun + deploy / Q2 RSC + Server Actions / Q3 Cache Components + Suspense / Q4 'use client' cascade + tree-shaking. Heavy research per user directive (~14 sources). **Q1: Next.js 16 production-grade in 2026-Q2** — released 2025-10-21, currently 16.2 (2026-03-18) with 400% faster `next dev` + 50% faster rendering + 200+ Turbopack bug fixes; Cache Components stable replacing experimental PPR; Turbopack stable as default; React 19.2 + React Compiler 1.0 stable; proxy.ts replaces middleware.ts; Min Node.js 20.9+. **Bun + Next.js 16 on Vercel = Public Beta NOT GA** (Vercel announcement 2025-10-28; 28% latency reduction in CPU-bound rendering; "working closely with Bun team to bring this capability to production"). **Deploy fork — recommend Vercel-for-cockpit + container-for-backend (split-deploy industry standard).** Cal.com production cite: 250k+ LOC, 100+ pages, 5-month App Router migration with Vercel Edge Config feature-flagging rollout; Vercel + container split is industry-default pattern (Stripe Dashboard, Linear, Cal.com all separate-deploy). **CVE-2025-55182 React2Shell (Dec 3 2025): critical pre-auth RCE in RSC Flight serialization protocol** — patched in Next.js 16.0.7+; active widespread exploitation observed by Google TIG + Microsoft + AWS + Palo Alto; vault as F-RSC-1 failure mode. **Q2: Server Actions for cockpit mutations + Route Handlers for SDK API (external clients)** — settled 2026 production-grade pattern. Better Auth + Next.js 16 RSC: `auth.api.getSession({ headers: await headers() })`; `nextCookies()` plugin auto-handles Set-Cookie; documented RSC limitation: "RSCs cannot set cookies, the cookie cache will not be refreshed until the server is interacted with from the client via Server Actions or Route Handlers." **Q3: Cache Components + Suspense streaming** — `cacheComponents: true` + `"use cache"` directive; sub-50ms TTFB on personalized routes; live-ops data uncached / per-request inside Suspense; slow-changing data cached via `"use cache"` + `cacheLife` profiles. **Q4: 'use client' cascade is real** — Next.js Issue #60246 (react-aria-components tree-shaking broken at 'use client' boundary), Discussion #13763 (36KB → 15KB after removing 'use client'); discipline: leaf-component placement, no wildcard imports, `optimizePackageImports` for lucide-react/date-fns/radix-ui, bundle analyzer in CI, <200KB gzipped initial JS target (Vikas Kumar 60% reduction case study). **Contradiction probe: TanStack Start migration cite (Melvin Prince Medium 2026-01-19, partial paywalled): 60% build-time reduction + hydration error class elimination after migrating FROM Next.js 16 TO TanStack Start; "App Router Fatigue."** Per Contradiction protocol: Cal.com TO App Router weight beats single anonymized FROM-cite at greenfield-MVP scale. **Triangulation:** ✓ all four sub-questions across production cites + docs + contradiction probes (~14 sources). Confidence: high. **Hands back to /design with explicit recommendation:** Next.js 16 + Vercel-for-cockpit (Node.js 22 default at Vercel — NOT Bun on Vercel due to Public Beta status) + Server Actions for mutations + Route Handlers for SDK + Cache Components opt-in caching + 'use client' discipline + monorepo via Turborepo (Cal.com pattern) coupling cockpit to backend modules via typed imports. **Backend stays on Bun + container per `[[backend-stack]]` amendment**; cockpit on Vercel-Node-22; the two deploy units communicate via HTTP for SDK API calls.
- [[backend-service-shape]] — type: decision — **backend service shape RESOLVED + AMENDED 2026-05-03 (runtime flipped per `[[backend-stack]]` amendment).** Modular monolith — single Bun 1.3+ TS process per replica (Node.js 22 LTS fallback CI-tested), deployed as container, **Hono HTTP framework**, ESLint blocks `import 'bun'` / `import 'bun:*'` in non-test files. **Module boundaries align 1-to-1 with vault feature boundaries:** `auth/` (Better Auth + player auth + SDK API key), `players/` (identity + DSR primitives), `wallet/` (per `[[wallet-mechanics]]`), `catalog/` (per `[[catalog-versioning]]`), `loot/` (per `[[loot-rng-construction]]`), `outbox/` (per `[[wallet-mechanics]]` §4), `cockpit/` (web UI), `sdk/` (HTTP API), `idempotency/` (per `[[idempotency-strategy]]`), `infra/` (RLS withTenant wrapper + telemetry). Plus cross-cutting `db/`, `lib/`, `types/`. **Module isolation discipline:** modules call each other via typed exports (function calls, not network); cross-module DB access via owning module's repository functions only; ESLint `import/no-restricted-paths` enforces. **Citadel-pattern reserve:** typed-interface boundary makes future module extraction mechanical, not architectural. **Outbox worker co-hosted** in same Node.js process at MVP (1-2 replicas), 250ms polling via SKIP LOCKED on `staged_jobs` per `[[wallet-mechanics]]`. **Container deployment** on PaaS (Railway/Render/Fly.io/Cloud Run — specific PaaS deferred to implementation phase). Always-on replica (no scaling-to-zero) at MVP for cold-start avoidance. **Out-of-scope at MVP (vaulted as rejected):** serverless edge (argon2 native binding + outbox long-running incompatible with V8 isolates), microservices (30-50% early-dev overhead unaffordable for solo dev), worker-as-separate-service from day one (<5 replicas SKIP LOCKED handles coordination), WAL-CDC outbox (deferred scaling path). **Seven rejected alternatives** (microservices, flat monolith, Workers/Vercel-Edge, Vercel-Functions/Lambda, separate-worker-from-day-one, WAL-CDC at MVP, per-module Postgres schema). **Eight failure modes** with mitigations (module-boundary erosion, outbox starves HTTP, deploy unit grows, SKIP LOCKED breaks at scale, premature interface stabilization, PaaS lock-in, cold-start latency, all-or-nothing deploy blast radius). **Ten cascade obligations** for implementation phase (module scaffold, export interface review, outbox poller, webhook HTTP client, telemetry, PaaS pick, Dockerfile, CI/CD, always-on config, feature flag infra). **Q1 user directive "Bun not Node" REJECTED ON EVIDENCE per `[[backend-service-shape-research]]` Findings Q1.1-Q1.3** — `[[backend-stack]]` Node.js 22 default stands as written; new evidence (OpenCode regret, ongoing Bun memory leak class, pkgpulse long-running-workload caveat) reinforces rather than amends.
- [[backend-service-shape-research]] — type: research — answers Q1 Bun-vs-Node re-research + Q2 backend service shape. **Q1 result: reaffirms `[[backend-stack]]` Node.js 22 default; does NOT warrant amendment to Bun-default.** Re-research surfaced new evidence: OpenCode founder Jay V publicly migrated FROM Bun TO Node citing memory + crashes + Windows support; multiple GitHub-tracked memory-leak class issues across Bun 1.1.x range continuing to surface in 2026 (Issues #16339, #18488, #24216, #25948); Bun 1.1.13 (April 2026, post-Anthropic-Dec-2025 acquisition) shipped memory fixes "following developer complaints" — acquisition signals engineering investment but stability work ongoing not closed; pkgpulse 2026 caveat *"long-running workloads amplifying problems that short benchmarks never reveal"* applies directly to BokChoy outbox worker. Better Auth Issue #2283 (Bun + Drizzle + PostgreSQL crash) **closed wontfix and `bunx`-CLI-specific (not runtime)** — moot for BokChoy because `[[backend-stack]]` commits drizzle-kit-only schema migrations. **Q2 result: modular monolith is unambiguous 2026 consensus for solo-dev B2B SaaS at MVP.** DHH 2023 *"the vast majority of systems are much better served by starting and staying with a monolith"* + 2026 X reaffirmation *"the majestic monolith remains undefeated... it should be the absolute last resort"*; GitHub + Shopify cited as production proof at scale; 2026 industry consensus: "for SaaS under $10M ARR, modular monolith is the smarter starting point unless 15+ engineers, distinct team ownership, or independent scaling needs from day one"; microservices add 30-50% overhead in early development. **Outbox worker co-hosted in monolith at <5 replicas via SKIP LOCKED** per `[[wallet-mechanics]]` `staged_jobs`; migration to separate-worker post-MVP at scale threshold; WAL-CDC alternative for further scale. **Serverless edge (Workers, Vercel Edge) ruled out** for BokChoy MVP — argon2 native binding (per `[[player-auth]]`) + outbox long-running pattern (per `[[wallet-mechanics]]`) both incompatible with V8-isolate constraint set. **Container deployment** on Supabase-managed Postgres + Railway/Render/Fly.io PaaS. **Triangulation:** ✓ across both sub-questions (9 sources, mixed-evidence honest read on Q1; convergent evidence on Q2). Confidence: high. Hands back to `/design` for `[[backend-service-shape]]` decision entry (modular monolith pick + outbox worker co-located + container deploy + module isolation discipline) and reaffirmation that `[[backend-stack]]` Node.js 22 stands.
- [[backend-stack]] — type: decision — **backend stack RESOLVED + AMENDED 2026-05-03 (runtime flipped to Bun + Hono locked + cross-runtime discipline).** TypeScript (strict mode) + **Bun 1.3+ as MVP runtime** (Node.js 22 LTS retained as documented fallback, Dockerfile variant CI-tested but not deployed) + **Hono HTTP framework** (Hono `serve` adapter, NOT `Bun.serve`) + Drizzle ORM (`pgPolicy`/`pgRole` schema declarations + BokChoy-side `withTenant(projectId, fn)` transaction wrapper executing `SELECT set_config('app.current_tenant', ...)` per tx, analogous to Supabase's `getDrizzleSupabaseClient`) + Better Auth (with Drizzle adapter, anonymous plugin for `[[player-auth]]` (γ) guest accounts, organization plugin for customer-developer multi-tenancy) + argon2id m=64MB t=3 p=4. **Multi-tenancy mapping (Stripe model):** Better Auth `organization` = customer-developer (auth plane); RLS `app.current_tenant` = `project_id` (data plane); `projects.organization_id` foreign key links them. **Schema migrations via `drizzle-kit` ONLY** — Better Auth's `getMigrations` does not support Drizzle adapter; BokChoy hand-writes Better Auth's user/session/account/organization tables in Drizzle schema files. **Cockpit + admin auth:** Better Auth email/password + magic-link (cockpit), Better Auth OAuth Google/GitHub (admin SSO); SDK API key is BokChoy-issued per project (HMAC-signed Stripe-pattern, independent of Better Auth). **Nine rejected alternatives** (Bun-as-default, Go/Rust/Elixir, Prisma, Kysely, Auth.js v5 for new project, Lucia, managed Clerk/WorkOS, project-as-org, nested-orgs). **Eight failure modes** with mitigations (`withTenant` bypass / RLS regression, Better Auth schema breaking change, Bun memory-leak class regression, drizzle-kit migration breakage at scale, Better Auth org plugin custom-field UI breakage, SDK API key compromise, customer-as-org breaks for agencies, Auth.js v5 security-patch SLA gap). **Eight cascade obligations queued for implementation phase** (withTenant + CI lint, Drizzle schema files for Better Auth + BokChoy tables, Better Auth config + argon2id params, SDK API key infra, deployment-matrix docs, drizzle-kit-against-direct-Postgres runbook, Better Auth upgrade procedure, integration tests). **Revisit-when conditions named** for Bun adoption, SCIM/SAML availability, agency-customer cohort, drizzle-kit production friction, Better Auth schema breakage, Lucia revival, cross-customer player portability, Node.js 22 EOL.
- [[file-structure]] — type: decision — **Cockpit folder structure RESOLVED 2026-05-11** from `[[cockpit-folder-structure-research]]`. **Pick: (P-CAL) Cal.com-style vertical-slice modules** at `apps/cockpit/modules/{feature}/{components,api,hooks,lib,types}/`. Three converging defenses: (1) internal consistency with `[[backend-service-shape]]` §2 backend `modules/{feature}/` naming — shared cross-codebase vocabulary; (2) Cal.com production-cited at largest scale surveyed (38 modules at 250k+ LOC); (3) long-term migration cost asymmetry — `[[mvp-feature-sequence]]` months 4-5 cockpit-core + month 6 live-ops mechanics → ~10 features by month 6, (P-LAYER) refactor window. **(P-LAYER) rejected on cite-count-not-cite-weight** — Documenso+Trigger.dev (2/4) ship at smaller scale untested at Cal.com's complexity. **(P-DUB) rejected on internal-consistency-loss** — Dub.co's `ui/{feature}/` + `lib/{feature}/` doesn't match backend `modules/`. **(P-VIEW) `*-view.tsx` page-view abstraction SKIPPED at MVP** — Cal.com (1/4) uses it; no convergence + no testability/reuse demand at slice 8.3 scope. App-level `page.tsx` directly composes module components. **Top-level structure:** `app/` (thin route definitions, route groups `(marketing)`/`(auth)`/`(app)`); `modules/{auth,projects,organizations,marketing}/` vertical slices; `components/{ui,layouts}/` shared cross-feature UI (shadcn primitives + cross-feature shells); `lib/{query-client,utils}.ts` cross-feature utilities. **NO barrels** per Vercel `bundle-barrel-imports` from `[[cockpit-stack-integration-research]]` Amendment F6.15. **NO workspace-level `packages/cockpit-features/` at MVP** — single-app context; over-engineering. **Conventions enforced via PR review:** 'use client' leaf placement; `React.cache` for cross-component memoization within request; `lib/session.ts` MUST use `React.cache(getSession)` never module-level mutable state per Vercel `server-no-shared-module-state`; shadcn imports by direct path; thin `app/page.tsx` (composition only). **4 rejected alternatives** with named winning conditions. **3 failure modes with mitigations** (empty modules at MVP — accepted; cross-module utility duplication — promote to `lib/utils.ts` when 2+ callers; shadcn primitive over-coupling — composition not forking). **6 revisit-when triggers** named. **10 cascade obligations for slice 8.3 + 8.4 implementation** enumerated. **Open thread:** cockpit testing infrastructure (Vitest/Playwright wiring deferred until first test gap surfaces; slice 8.2.1 shell-smoke-script precedent continues at slice 8.3).
- [[cockpit-folder-structure-research]] — type: research — answers /design pushback "let's look at how the folder structure will be under cockpit" + user's "let's look at one production project let go back research" enforcing the ≥2-cite floor. 4 production B2B SaaS cockpits source-walked: Cal.com (Next.js, `apps/web/modules/{feature}/` vertical-slice), Dub.co (Next.js, `apps/web/ui/{feature}/` + `apps/web/lib/{feature}/` horizontal split-by-feature), Documenso (Remix, `apps/remix/app/{components,providers,routes,storage,types,utils}/` layer-based + workspace `packages/{api,auth,ui,lib}/` extracted), Trigger.dev (Remix, `apps/webapp/app/{components,hooks,models,presenters,services,routes}/` layer-based). **F1 LOAD-BEARING: NO convergent production pattern across 4 cites.** 50% (2/4) ship LAYER-BASED at app-level. 25% (Cal.com 1/4) vertical-slice modules. 25% (Dub.co 1/4) horizontal split-by-feature. The "feature-folder is the production-cited norm" claim from prior /design turn is FALSIFIED. **F2:** Bulletproof React (alan2207/bulletproof-react ~16k stars) is tier-4 educational template advocacy, NOT a production cite. Prior /design turn over-weighted it as if production-cited × 1. Walked back. 0/4 production cites ship strict Bulletproof shape `src/features/{feature}/{components,api,hooks,types,utils,routes}/`. **F3:** workspace-level feature/domain extraction is partial convergence (Cal.com + Documenso = 2/4) — correlates with "cross-app reuse expected" (multiple Next.js apps). For BokChoy single-cockpit-app MVP: workspace extraction is over-engineering; revisit when marketing splits or admin-app emerges. **F4 tradeoff matrix:** feature-folder wins on cohesion + onboarding + refactoring boundaries; layer-based wins on simplicity at small scale + naturally shared cross-feature primitives + no empty modules at MVP. **F5:** Dub.co's horizontal split is a distinct 3rd path matching TS+React stacks with TanStack Query + RSC data/UI separation. **C1 contradiction:** "feature-folder is production norm" falsified by 50% layer-based finding. **C2:** 2 of 4 cites are Remix not Next.js — cross-framework cites add weight on framework-agnostic axes (layer-vs-feature) but reduce weight on Next.js-specific axes. **Operational implications: three honest paths for `[[cockpit/file-structure]]` /design pick** — (P-CAL) Cal.com vertical-slice `modules/{feature}/{components,hooks,api,*-view.tsx}/`; (P-DUB) Dub.co horizontal split `ui/{feature}/` + `lib/{feature}/`; (P-LAYER) Trigger.dev/Documenso layer-based `{components,hooks,services,...}/`. (P-LAYER) has 2/4 production cites (majority); (P-CAL) + (P-DUB) have 1/4 each. For BokChoy solo-dev-MVP with 3-5 features at slice 8.3-8.6 scope, (P-LAYER) is lowest-LOC + highest-cite path. Migration to feature-folder worth the cost as cockpit grows past ~10 features (post months 4-5 cockpit-core). /design picks with named winning conditions. **Vercel anti-pattern table from `[[cockpit-stack-integration-research]]` Amendment F6 applies regardless of pick** (no barrels, leaf 'use client', etc.). 5 open threads named. Confidence: high. Formbricks clone failed (5th candidate); 4 cites cleared the *Industry-standard* gate floor.
- [[cockpit-stack-integration-research]] — type: research — answers slice 8.3 (cockpit β-shape) two-axis query: (Q-A) Better Auth + Next.js 16 + split-deploy + CORS + cookies, (Q-D) RSC composition + anti-pattern audit. 7 sources triangulated (Better Auth demo Next.js + Hono integration docs + cookies/concepts docs + Next.js integration docs + Next.js 16.2.6 fetching-data docs + Cal.com production code + TanStack Start contradiction probe). **F1 LOAD-BEARING: reverse-proxy split-deploy pattern is production-cited via Better Auth `concepts/cookies.mdx` + Vercel `rewrites` example.** Cockpit `vercel.json` proxies `/api/auth/:path*` + `/v1/:path*` to backend; browser sees first-party requests; cookies work without SameSite=None/Safari-ITP complexity. **F2:** shared-parent-domain (P2) rejected for BokChoy — wins only when infrastructure forces distinct hosts; (P1) reverse proxy is the simpler MVP path. **F3 LOAD-BEARING:** Better Auth+Hono mount is canonical 3 LOC: `app.on(["POST", "GET"], "/api/auth/*", (c) => auth.handler(c.req.raw))`. Source 1 verbatim. **F4:** cockpit has NO Better Auth INSTANCE — instance + DB connection live only on backend; cockpit uses `better-auth/react` CLIENT only; calls backend via Vercel-rewrite. **F5 RSC composition canonical patterns:** parallel independent fetches via `Promise.all` (Cal.com pervasive, 12+ pages observed); sequential dependent fetches via `await ... await` with downstream `<Suspense>` (Next.js docs explicit-ACCEPTABLE); `<Suspense>` granular > `loading.tsx` route-level (Next.js docs recommendation); layout uncached-data anti-pattern (blocks navigation, not loading.js); Client Component data via `use(promise)` API + promise prop-passing; `React.cache` for cross-component memoization. **F6 12-row anti-pattern table with named fixes** — extends `[[frontend-stack]]`/`[[tanstack-query-rsc-research]]` enforcement list: (1) cascaded `'use client'` (2) sequential awaits for independent data (3) layout uncached without local Suspense (4) page-level Suspense for partially-slow content (5) `getSessionCookie` for authz (6) `fetchQuery` SSR (7) staleTime=0 (8) double-invalidate (9) TanStack Query owning all fetching (10) Server Action signIn without `nextCookies()` (11) `'use client'` on outer layout (12) foreign-domain cookies without proxy/cookie-attribute config. **F7 OAuth-primary sign-in form pattern:** Client Component buttons calling `authClient.signIn.social({provider})`; email/password fallback via small form; no magic-link at MVP per design pick (iii) 2026-05-11. **C1 contradiction walked back:** earlier WebFetch "Better Auth split-deploy not documented" finding falsified — cookies/concepts page + Hono integration page BOTH document split-deploy. **C2 cite-class amendment owed to `[[backend-stack-research]]` Source 8:** Cal.com listed as Better Auth production user; reality is `next-auth 4.24.13` (security-patch-only, Better Auth migration target). Cal.com retains weight as RSC composition reference only. **Operational implications for slice 8.3 implementation: vercel.json rewrites; backend Hono mount; trustedOrigins extension in auth-config; Better Auth React client at cockpit; RSC session-read via `React.cache(fetch '/api/auth/get-session')`; section-level Suspense composition; 6 anti-patterns enforced via PR review; ~10 cascade obligations enumerated for slice 8.3 prep.** Confidence: high.
- [[admin-auth-surface]] — type: decision — **admin auth surface RESOLVED 2026-05-11 from `[[better-auth-org-admin-research]]`.** Customer-developer-admin endpoints (`bootstrapProjectReasonCodes`, `walletDeidentifyPlayer`, future cockpit admin handlers across slices 8.2-8.5) gated through five sub-decisions: **(D1) `organizationId` resolution = body precedence + session fallback** (`ctx.body.organizationId ?? ctx.query.organizationId ?? session.session.activeOrganizationId`; 400 if neither) — production-cited × 10 Better Auth call sites at commit `e21d744`. **(D2) Gate shape = Hono middleware factory `adminGate({ resource, actions })`** — registered per-route, sets typed `c.var['admin.member']: Member` + `c.var['admin.org']: Organization` via Hono `Variables` generic; matches BokChoy internal precedent × 3 prior slices (api-key, idempotency, error-middleware); first-class Hono composition primitive; idiom-cited via `idioms/typescript.md` *Make impossible states unrepresentable*. **(D3) Access control mode = Static AC** — statements + roles defined at startup in `packages/auth-config/src/index.ts` via `createAccessControl({...})` + `roles: { admin: ... }`; dynamic AC + `organizationRole` table deferred to RBAC-plugin slice post-MVP per `[[backend-stack]]` §7. **(D4) Schema scope at MVP =** keep `user`/`session`/`account`/`verification` + `organization`/`member`/`invitation` (already in `packages/db/src/schema/auth.ts`); NO `team`/`teamMember`/`organizationRole` tables added — direct corollary of D3 + `[[backend-stack]]` §7 customer-team-member deferral. **(D5) MVP statement + role set =** `{ reasonCode: ['bootstrap'], player: ['deidentify'] }` + single `"admin"` role granting both; 2-line edit per new admin endpoint thereafter. **5-step `adminGate` contract:** (1) `auth.api.getSession()` → 401 BC401 if null; (2) resolve org per D1 → 400 BC400 if missing; (3) BokChoy-side tenancy check `project.organization_id === resolvedOrgId` → 403 BC403 cross_org_forbidden; (4) `db.select().from(member)...` → 403 BC403 not_a_member; (5) `auth.api.hasPermission({...})` → 403 BC403 insufficient_permissions. Sets `c.set('admin.member', ...)` + `c.set('admin.org', ...)` + `next()`. **OTel span emitted** with `auth.{organizationId,userId,role,resource,actions,outcome}`; page on `outcome != 'pass'` > 5% over 5min per project. **Six rejected alternatives** with named winning conditions: (β) session-only D1, (γ) body-only D1, (ν) inline-per-handler D2, (ξ) helper-function-inline D2, dynamic AC at MVP D3, multi-role split D5. **Three failure modes named with mitigations:** primary (`member.role` references non-existent role string — mitigated by CI lint `scripts/check-auth-roles.ts`); secondary (middleware context-key collision — mitigated by `admin.` namespace prefix); tertiary (`[[backend-stack]]` F5 re-emergence — already cleared per `[[better-auth-org-admin-research]]` F5-clearance). **Six revisit-when triggers** named (dynamic AC, multi-role split, session-only D1, inline D2, organizationRole/team tables, Better Auth org plugin rewrite stabilization). **Open threads:** path-discrepancy cleanup (`apps/auth-config/` → `packages/auth-config/` cascade owed to `[[tenancy-ids-research]]:256` + `[[wallet-mechanics]]` A12 + state.md); D2-(μ) production-cite gap (no public B2B SaaS Better-Auth+Hono+middleware-factory source-walked — confidence medium-high vs high); BC400/BC401/BC403 allocation in `[[wallet-mechanics]]` A18 owed when first admin handler ships.
- [[better-auth-org-admin-research]] — type: research — answers /design Q2 (Better Auth org plugin admin gate shape) for slice 8.2 customer-developer-admin endpoints (`bootstrapProjectReasonCodes`, `walletDeidentifyPlayer`). **F1 (LOAD-BEARING): canonical admin gate = `hasPermission()` called manually at handler top** — production-cited via source-walk of `better-auth/better-auth` org plugin (commit `e21d744`, v1.6 HEAD 2026-05-11): every protected route in Better Auth's own code (crud-org / crud-team / crud-members / crud-invites / crud-access-control) follows `findMemberByOrgId` → `hasPermission({ permissions, role: member.role, options, organizationId }, ctx)` → throw FORBIDDEN. **No middleware decorator exists** — manual call is the intended pattern; docs (better-auth.com/docs/plugins/organization v1.6) confirm. **F2:** Session carries `activeOrganizationId` ONLY (set via `setActiveOrganization()` route or auto-on-create); role/member NOT on session — separate adapter call required per handler. In-memory `cacheAllRoles` exists for repeated `hasPermission` calls in same request. **F3:** Default statements (`organization` / `member` / `invitation` / `team` / `ac`) don't include BokChoy resources (`project`, `wallet`, `player`, `reasonCode`); BokChoy MUST extend via `createAccessControl({ reasonCode: ["bootstrap"], player: ["deidentify"], ... })` + custom roles in `apps/auth-config/`. **F4:** Multi-role via comma-separated `member.role` string; OR-of-roles semantics built-in (free headroom, not required at MVP). **(P1) settled as production-cited pattern**: `member.role === "member"` (default) has ZERO permissions; only `"admin"` / `"owner"` (or extended role) passes. (P2) any-org-member-equally-privileged is a regression vs defaults, undefended in surveyed source. **C1 contradiction:** org plugin mid-rewrite across 6 in-flight PRs (#7251, #7544, #7591, #7601, #7628, #7886) through 2026-Q1/Q2 — v1.6 stable, pin Better Auth version, expect minor-version migration work on next bump. **C2 contradiction (resolved):** demo app uses `session.user.role` for instance-admin — that's the SEPARATE `admin` plugin, not org plugin. BokChoy customer-developer-admin = org plugin's `member.role`; instance-admin (if ever shipped) = `admin` plugin's `user.role`. **Operational implications for slice 8.2:** both `bootstrapProjectReasonCodes` and `walletDeidentifyPlayer` share 5-step gate (session → resolve org → BokChoy-side tenancy check on `project.organization_id` → `findMemberByOrgId` → `hasPermission`); reusable Hono middleware factory `adminGate({ resource, actions })` defensible for ≥3 admin handlers. **Schema additions owed:** Better Auth's `organization`/`member`/`invitation`/`team`/`teamMember`/`organizationRole` Drizzle definitions (already vaulted in `[[tenancy-ids-research]]`); BokChoy `createAccessControl()` config in `apps/auth-config/src/index.ts` with custom statements + extended roles. **Open design follow-ups (NOT research):** (a) session-context posture pick — require `organizationId` in body/query vs require `activeOrganizationId` on session; (b) middleware-factory vs inline-per-handler shape pick. **F5 risk cleared:** Better Auth custom-field UI breakage (vaulted in `[[backend-stack]]` F5) is bundled-UI-only; server-side `hasPermission()` flow unaffected. Triangulation: ✓ Source 1 (production code, source-walked) + Source 2 (docs v1.6) + Source 3 (rewrite PRs, stability signal) + Source 4 (demo app contradiction probe, resolved). Confidence: high.
- [[backend-stack-research]] — type: research — answers Q1 backend language + Q2 ORM + Q3 auth library for BokChoy MVP. **Q1: TS-on-backend has tier-1 production-cite via Trigger.dev firestarter Bun migration (5× throughput, named workload, named engineer); Cal.com + Deel.com confirmed via Better Auth founder citation as production users. Bun production-readiness real with documented edge cases (HTTP-model memory leak, since fixed); Node.js 22 conservative MVP default, Bun opt-in upgrade.** **Q2: Drizzle wins on RLS-first-class — `pgPolicy`+`pgRole` schema declarations generate native Postgres RLS during migration. Prisma is workaround-only stack (Issue #12735, Yates library, Prisma Client Extensions with documented nested-transaction breakage).** Drizzle does NOT auto-manage `SET LOCAL` — BokChoy implements transaction wrapper analogous to Supabase's `getDrizzleSupabaseClient`. **Q3: Better Auth has consolidated TS auth library landscape — Lucia deprecated 2025-03 (maintainer Pilcrow sunset), Auth.js team joined Better Auth 2025-09-22 (now security-patch-only mode). v1.5.0 (2026-03-01): 600+ commits, 70 features, 220 bug fixes. Anonymous plugin maps directly onto `[[player-auth]]` (γ) guest-account shape via `generateRandomEmail`. Production cites: Cal.com, Deel.com, dough.ink, MeetingBaas, ChatGPT/Google Labs (via Auth.js).** **WorkOS contradiction probe (skin in the game, 2026-03-02 Maria Paktiti):** Better Auth lacks SAML/OIDC enterprise SSO, SCIM, audit logging, pre-built UI; "limited multi-tenancy support requiring significant custom architecture." None of those gaps bind for BokChoy indie/SMB MVP wedge ICP; SCIM/SAML closes enterprise-tier door post-MVP unless WorkOS-layered. **Triangulation:** ✓ all three sub-questions across production cites + docs + contradiction probes (11 sources). **Coupling resolved:** TS+Drizzle+Better Auth is the natural stack pairing — pre-pick was correct on conclusion, undefended on substance; this research provides the substance. **Open threads:** Better Auth project↔organization mapping for BokChoy two-tier (customer + project), Bun vs Node22 MVP default decision, SCIM/SAML roadmap watch, drizzle-kit migration scale signal.
- [[local-docker-roles-research]] — type: research — answers five questions blocking `[[local-docker]]` amendment after sub-unit (2) surfaced gaps. **Q1 (managed prod role layout):** verified via live Supabase Free-tier query — `postgres` is `NOSUPERUSER, INHERIT, CREATEROLE, CREATEDB, LOGIN, REPLICATION, BYPASSRLS`; `supabase_admin` is the only SUPERUSER; 14 non-pg roles total. **Q2 (CLI bootstrap SQL):** `supabase/cli` `internal/utils/templates/globals.sql` is the canonical bootstrap; line 30-31 commented form documents the intended `postgres` shape (matches Q1). CLI `restore.sh:25` rewrites to SUPERUSER for local-CLI dev mode only — does NOT match managed; per *Contradiction protocol*, managed wins for BokChoy. **Q3 (`POSTGRES_USER` override):** Dockerfile-17:176 hardcodes `supabase_admin`; overriding breaks init scripts that reference it. Don't override. **Q4 (default extensions on managed):** managed has pgcrypto + pg_stat_statements + uuid-ossp + supabase_vault auto-installed in `extensions`/`vault` schemas; standalone has none — `schema-17.sql` declares them but is not executed during initdb because Dockerfile-17:158 puts it in a subdir of `/docker-entrypoint-initdb.d/` that the entrypoint doesn't recurse into. Falsifies `[[local-docker]]` Reasoning's "auto-installed" claim. **Q5 (log spam source):** Dockerfile-17:187 healthcheck `pg_isready -U postgres` runs every 2s and FATAL-logs because `postgres` doesn't exist standalone. Creating `postgres` silences it. **Operational implications for `[[local-docker]]` amendment:** add `00-roles.sql` (creates `postgres` per Q1 attributes + `bokchoy_app` non-owner per `[[wallet-mechanics]]` §8); install pgcrypto in `extensions` schema (skip pg_stat_statements, uuid-ossp, supabase_vault as out-of-MVP-scope); don't override POSTGRES_USER; first-init recovery is `down -v` + `up -d`; add F-Local-6 (first-init transient log-spam window). 8 sources triangulated.
- [[local-docker]] — type: decision — **local Docker pick RESOLVED 2026-05-04.** Path B locked: standalone `supabase/postgres:17.6.1.113` (348MB) + Mailpit via project-owned `compose.yml`. Backend + cockpit native via `bun --hot`. Direct Postgres connection (no Supavisor locally — `[[backend-stack]]` cascade-10 `prepare: false` CI lint defuses the parity-gap concern). Vitest pool=forks singleFork=true with per-test BEGIN/ROLLBACK + `withTenant()` SAVEPOINT inheritance. **pg_partman managed-tier verification: SUCCEEDED** on Supabase Free-tier 2026-05-04 — `[[host-platform]]` cascade-1 closed; 6-month-stale ambiguity resolved. Path A (`supabase start -x`) rejected — CLI version-drift coupling not worth Mailpit-included trade now that prepare: false CI lint exists. Path C (stock+custom Dockerfile) rejected — parity-drift maintenance burden with no production cite. Five failure modes named with mitigations (image format drift, Supavisor non-prepared-statement constraint, Mailpit version drift, volume loss, Bun WSL2 file-watch). Six revisit-when triggers (non-Postgres dep, team growth, test-suite size, Supabase contract change, container-wrapped backend, prod off Supabase).
- [[drizzle-orm-research]] — type: research — answers four Drizzle behavior questions for BokChoy MVP. **(1) `pgPolicy` round-trip works** — `drizzle-kit migrate` generates correct `CREATE POLICY ... AS PERMISSIVE FOR ... TO ... USING (...) WITH CHECK (...)` + auto-`ENABLE ROW LEVEL SECURITY`; round-trip tested in `drizzle-kit/tests/rls/pg-policy.test.ts`. **GAP: `FORCE ROW LEVEL SECURITY` is NOT generated** (zero matches in `sqlgenerator.ts`). NEW cascade obligation for `[[backend-stack]]`: CI-enforced post-migration script that asserts FORCE RLS on every table with policies (closes `[[wallet-mechanics]]` §8 owner-bypass defense). **(3) `prepare: false`** is set on postgres-js client construction (`postgres(connStr, { prepare: false })`), Drizzle wraps the client unchanged — flow-through verified via `drizzle-orm/src/postgres-js/driver.ts`. Required for Supavisor unconditionally; commit local + prod identical. **(4) Nested transactions correctly use SAVEPOINT** — `tx.transaction(innerFn)` calls `client.savepoint()` from postgres-js (`drizzle-orm/src/postgres-js/session.ts`). `withTenant(projectId, fn)` inside test transaction-rollback wrapper round-trips correctly; outer ROLLBACK cleans inner SET LOCAL GUC. **(5-reframed) N+1 is NOT a Drizzle failure mode** (Drizzle docs verbatim: "a single SQL statement is outputted by Drizzle"). The actual perf risk is single-query-with-poor-plan at scale — Issue #5245 (open, 2026-01-10, RQB-v2 / Drizzle v1:beta8) documents pathological `LEFT JOIN LATERAL + json_arrayagg` plans on 3-table joins + filtered-related-where + concurrent load (reporter: 650k orders × 1.1M items, "kills DB after a few hours"). Mitigations: (a) CTE rewrite, (b) drop to `db.select().leftJoin(...)` core builder, (c) wait for upstream fix. Profile-then-mitigate posture; do NOT pre-emptively raw-SQL. **NEW finding: Drizzle v1.0.0-rc.1 (2026-04-30, 3 days prior) — v1 NOT GA. Last stable 0.45.2.** Recommendation lane: pin 0.45.x stable for MVP launch (avoids #5245's RQB-v2-specific shape); track v1 GA for post-launch upgrade. 11 sources triangulated. 5 cascade obligations queued for `[[backend-stack]]` + `[[wallet-mechanics]]` validation. Hands back to /design for FORCE-RLS workaround pick + version pin.
- [[local-docker-research]] — type: research — answers Q1 `supabase/postgres` ships pg_partman 5.3.1 + pgcrypto/pg_stat_statements/uuid-ossp auto-installed (verified `nix/ext/versions.json` + `nix/tests/expected/extensions_schema.out` 2026-05-03); Docker Hub `supabase/postgres:17.6.1.113` is **348MB**; `supabase start -x` excludes 13 services (db always on, mailpit + supavisor bundled). Q2 three viable paths surfaced: **Path A** `supabase start` with exclusions (full prod parity, supavisor + mailpit included, CLI-coupled), **Path B** standalone `supabase/postgres` via custom compose (348MB, no supavisor, mailpit added separately), **Path C** stock `postgres:17-alpine` + custom Dockerfile (drift risk, no production cite). No engineering blog found of teams ditching `supabase start` for stock — dominant pattern is Supabase-official. Q3 Supavisor: bundled with Path A only; **NEW cascade obligation for `[[backend-stack]]`**: postgres-js requires `prepare: false` for transaction-mode pooling (Supabase docs verbatim Source 9). Q4 integration tests: **PGLite OUT** (no pg_partman support — server-side extension); Drizzle Discussion #5140 recommends schema-per-test (parallel) or transaction-rollback (sequential); transaction-rollback is natively compatible with `SET LOCAL` via SAVEPOINT semantics; pg_partman setup once in `beforeAll`. **LOAD-BEARING CONFLICT**: pg_partman ships in repo binaries + Docker Hub image, BUT Supabase managed Postgres prod tier availability **unverified** as of 2026-05-03 — last public staff statement (samrose 2025-11-13): "in progress, no firm date"; Snap Cloud (Supabase-powered) tier-5 doc in 2026-Q1 suggests shipped but no Supabase-official tier-1/2 confirmation found. **Cascade-1 from `[[host-platform]]` NOT closed by local image alone — requires verification against real Supabase Free-tier project before vaulting Path B.** Cautionary cite (Source 11 `lrfds/IncentivFlow`) shows wrong-shape `set_config(...,true)` outside transaction — code review checklist obligation. 13 sources triangulated. Hands back to /design for image pick + verification of pg_partman managed-tier availability.
- [[player-auth]] — type: decision — **player auth model resolved.** Owned-only at MVP (BokChoy stores credentials), pass-through deferred post-MVP demand-gated on first-paying-customer-with-live-game-requests-it. Per-project players (no cross-project portability — preserves processor-stance per EDPB 07/2020). **Minimal-PII (i)/(γ):** `players` schema is `(id, project_id, email NULL, password_hash NULL, created_at, last_login_at, locale NULL, under_13 NULL)`; guest play is the default (no email, no password); email captured opt-in at IAP/recovery. Argon2id (m=64MB, t=3, p=4 initial). **Compliance:** processor-stance for GDPR (customer = controller per `[[auth-compliance-research]]`), customer = COPPA operator + BokChoy = third-party vendor under §312.2 service-provider exception, CCPA Service Provider. **No contractual age-gate** — family-aimed customers in scope; `projects.is_child_directed` flag + per-player `under_13` flag per Unity GS pattern; under_13 players prevented from email population. **Required MVP infra:** Article 28 DPA template + SCC (2021 Module 2 C-to-P) + COPPA written-assurance template per 2025 amendments + security questionnaire response (~1-2 weeks legal infra copying public PlayFab/Unity templates); DSR primitives `bokchoy.player_export` + `bokchoy.player_erase` (extends `[[deidentify-mechanism-research]]` M2 stored-function pattern); column-level GRANT discipline + bulk-export rate-limiter + audit logging on PII columns. Six rejected alternatives named (pass-through-only, both-at-MVP, standard-PII (ii), controller-stance, contractual age-gate, cross-project players). Seven failure modes named with mitigations (credential stuffing, guest hijacking, customer-side credential logging, audience misdeclaration, RLS regression, role over-grant on PII, DSR export flooding). **Cascade obligations:** verify Supabase encryption-at-rest default; CI lint extends `[[wallet-mechanics]]` + `[[multi-tenant-rls-research]]` discipline to players table; pre-implementation security review of auth endpoints; runbook items for credential-stuffing response + DSR async pattern. **Library + ORM + backend language decisions DEFERRED to next entry** — language-stack idioms (argon2id implementation, session-token format, rate-limiter) are stack-conditional.
- [[auth-compliance-research]] — type: research — answers the GDPR controller-vs-processor + COPPA operator-vs-service-provider question for B2B game-backend SaaS storing end-user (player) PII. **Processor-stance is the universal industry pattern** across PlayFab/Microsoft (SCC Annex I: "data exporter as Controller and PlayFab as Processor"), Unity Gaming Services ("Under GDPR, Unity is the Processor and you, the developer, are the Controller"), and Heroic Labs ("Our Customers are the data controllers of all End User Data"). EDPB Guidelines 07/2020 framework supports: customer-developer determines essential means (which players, what fields, what retention), SaaS determines non-essential means (technical infra). FTC Apitor/JPush enforcement (2025-09) confirms operator-stance lands on the customer-developer, not the B2B SDK vendor — verbatim FTC principle: *"outsourcing functionality does not outsource liability."* 2025 COPPA amendments (effective 2025-06-23, full compliance 2026-04-22) raise the contractual bar: operators must conduct due diligence on third parties + obtain written assurances of reasonable security. **Industry pattern on under-13:** vendor disclaims direct end-user relationship + ships COPPA-compliance primitives (Unity dashboards toggles, Nakama DSR export/delete) + customer-controller carries operator obligation; no surveyed vendor contractually excludes under-13 audiences. **Operational implications for BokChoy:** processor-stance compatible with (ii) standard-PII; required infrastructure includes Article 28 DPA template + SCC + DSR-fulfillment primitives + sub-processor flow-down + breach-notify-customer (Article 33(2)) cascading to existing `[[wallet-mechanics]]` webhook posture; `[[deidentify-mechanism-research]]` HMAC-SHA-256 + key-destruction-as-erasure pattern survives stance-agnostic. Hands back to `/design` for the (ii)/(iii) decision and downstream library/ORM/language picks.
