// Cockpit module per [[backend-service-shape]] §2. Web UI routes for admin +
// customer-developer dashboards (the cockpit feature itself ships out of
// apps/cockpit; this module exposes the cockpit-facing HTTP endpoints).

export {};
