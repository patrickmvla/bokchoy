// Auth module per [[backend-service-shape]] §2 + [[player-auth]] + [[backend-stack]].
// Better Auth setup (anonymous + organization plugins), player auth flows,
// argon2id, SDK API key validation.
//
// Consumes @bokchoy/auth-config for the Better Auth factory.

export {};
