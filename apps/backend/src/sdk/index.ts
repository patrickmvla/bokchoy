// SDK module per [[backend-service-shape]] §2. SDK-facing HTTP API endpoints
// (player + game-server calls). API key validated via auth/ module per
// [[backend-stack]] customer-as-org SDK key pattern.

export {};
